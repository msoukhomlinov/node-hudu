[
  {
    "tool": "hudu_activity_logs_deleteAll",
    "field": "name",
    "newValue": "hudu_delete_all_activity_logs",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_all_activity_logs",
    "field": "title",
    "newValue": "Delete All Activity Logs",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_all_activity_logs",
    "field": "description",
    "newValue": "Delete activity logs. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: BULK \u2014 removes every activity log from the given datetime on; there is no narrower endpoint, and an unbounded call is refused (POLICY_DENIED). Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff).",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_all_activity_logs",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_activity_logs_findByResource",
    "field": "name",
    "newValue": "hudu_find_activity_logs_by_resource",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_activity_logs_by_resource",
    "field": "title",
    "newValue": "Find Activity Logs By Resource",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_activity_logs_by_resource",
    "field": "description",
    "newValue": "List the activity log entries for one resource. Preferred over activity_logs.listAll for 'what happened to this record'. Returns one record: a compact ActivityLogSummary (updated_at, \u2026 dropped); expand: true returns the full ActivityLog. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_activity_log for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_activity_logs_by_resource",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_activity_logs_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto activity_logs.findByResource, exactly like hudu_find_activity_logs_by_resource (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_find_activity_logs_by_resource is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_activity_logs_resolve",
    "field": "name",
    "newValue": "hudu_get_activity_log",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_activity_log",
    "field": "title",
    "newValue": "Get Activity Log",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_activity_log",
    "field": "description",
    "newValue": "Resolve an activity log entry by its id. Use activity_logs.findByResource instead whenever the resource is known - it uses server filters and needs no scan. Returns one record: a compact ActivityLogSummary (updated_at, \u2026 dropped); expand: true returns the full ActivityLog. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_activity_log",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_api_info_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto api_info.resolve, exactly like hudu_get_api_info (the retained name). hudu_get_api_info is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_api_info_resolve",
    "field": "name",
    "newValue": "hudu_get_api_info",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_api_info",
    "field": "title",
    "newValue": "Get API Info",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_api_info",
    "field": "description",
    "newValue": "Return the API information document. Prefer api_info.get; this exists so every resource satisfies the same floor. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_api_info",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_articles_archive",
    "field": "name",
    "newValue": "hudu_archive_article",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_archive_article",
    "field": "title",
    "newValue": "Archive Article",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_archive_article",
    "field": "description",
    "newValue": "Archive a Knowledge Base Article. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to erase: hudu_delete_article is the destructive path.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_archive_article",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_articles_create",
    "field": "name",
    "newValue": "hudu_create_article",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_article",
    "field": "title",
    "newValue": "Create Article",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_article",
    "field": "description",
    "newValue": "Create a Knowledge Base Article. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_article.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_article",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_articles_delete",
    "field": "name",
    "newValue": "hudu_delete_article",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_article",
    "field": "title",
    "newValue": "Delete Article",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_article",
    "field": "description",
    "newValue": "Delete a Knowledge Base Article. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_article keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_article",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_articles_findBySlug",
    "field": "name",
    "newValue": "hudu_find_articles_by_slug",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_articles_by_slug",
    "field": "title",
    "newValue": "Find Articles By Slug",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_articles_by_slug",
    "field": "description",
    "newValue": "Find one article by its URL slug. Preferred over articles.list for a slug lookup. Returns one record: a compact ArticleSummary (content, url, object_type, \u2026 dropped); expand: true returns the full Article. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_article for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_articles_by_slug",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_articles_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto articles.resolve, exactly like hudu_get_article (the retained name). hudu_get_article is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_articles_getContext",
    "field": "name",
    "newValue": "hudu_get_article_context",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_article_context",
    "field": "title",
    "newValue": "Get Article Context",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_article_context",
    "field": "description",
    "newValue": "Fetch an article with its company and folder context. Preferred over three separate calls when an agent starts from an article. Returns ArticleContext. Bounded: every sub-list uses limit (default 25, max 100) \u2014 no sub-fetch walks the account. Not for paging: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_article_context",
    "field": "annotations.bounded",
    "newValue": "default 25 records per sub-list, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_articles_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto articles.search, exactly like hudu_search (the retained name). hudu_search performs the same bounded search with body-aware ranking and snippets. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_articles_resolve",
    "field": "name",
    "newValue": "hudu_get_article",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_article",
    "field": "title",
    "newValue": "Get Article",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_article",
    "field": "description",
    "newValue": "Resolve a knowledge-base article from an id, name or slug. Use when the article identifier is a name or slug; articles.get is cheaper for a known id. Returns one record: a compact ArticleSummary (content, url, object_type, \u2026 dropped); expand: true returns the full Article. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_article",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_articles_search",
    "field": "name",
    "newValue": "hudu_search_articles",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_search_articles",
    "field": "title",
    "newValue": "Search Articles",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_search_articles",
    "field": "description",
    "newValue": "Search knowledge-base articles by a free-text query. Preferred over articles.list for any text search. Returns rows: a compact ArticleSummary (content, url, object_type, \u2026 dropped); expand: true returns the full Article. Bounded: limit defaults to 25 and is hard-capped at 100; a larger value throws CONFIG_ERROR and is never silently clamped. Do not use it when you already have an id, slug or domain: call hudu_get_article instead.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_search_articles",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_articles_unarchive",
    "field": "name",
    "newValue": "hudu_unarchive_article",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_unarchive_article",
    "field": "title",
    "newValue": "Unarchive Article",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_unarchive_article",
    "field": "description",
    "newValue": "Unarchive a Knowledge Base Article. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it on a record that is not archived: call hudu_update_article to change fields.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_unarchive_article",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_articles_update",
    "field": "name",
    "newValue": "hudu_update_article",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_article",
    "field": "title",
    "newValue": "Update Article",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_article",
    "field": "description",
    "newValue": "Update a Knowledge Base Article. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_article.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_article",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_asset_layouts_create",
    "field": "name",
    "newValue": "hudu_create_asset_layout",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_asset_layout",
    "field": "title",
    "newValue": "Create Asset Layout",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_asset_layout",
    "field": "description",
    "newValue": "Create an Asset Layout. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_asset_layout.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_asset_layout",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_asset_layouts_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto asset_layouts.resolve, exactly like hudu_get_asset_layout (the retained name). hudu_get_asset_layout is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_asset_layouts_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto asset_layouts.resolve, exactly like hudu_get_asset_layout (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_asset_layout is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_asset_layouts_resolve",
    "field": "name",
    "newValue": "hudu_get_asset_layout",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_asset_layout",
    "field": "title",
    "newValue": "Get Asset Layout",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_asset_layout",
    "field": "description",
    "newValue": "Resolve an asset layout from an id, name or slug. Use when the identifier is a layout name or slug; asset_layouts.get is cheaper for a known id. Returns one record: a compact AssetLayoutSummary (icon_color, sidebar_folder_id, include_passwords, \u2026 dropped); expand: true returns the full AssetLayout. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_asset_layout",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_asset_layouts_update",
    "field": "name",
    "newValue": "hudu_update_asset_layout",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_asset_layout",
    "field": "title",
    "newValue": "Update Asset Layout",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_asset_layout",
    "field": "description",
    "newValue": "Update an Asset Layout. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_asset_layout.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_asset_layout",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_asset_passwords_archive",
    "field": "name",
    "newValue": "hudu_archive_asset_password",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_archive_asset_password",
    "field": "title",
    "newValue": "Archive Asset Password",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_archive_asset_password",
    "field": "description",
    "newValue": "Archive a Password. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Sensitive: it handles credential material \u2014 never echo a secret value into a log, a description or an error (the SDK redacts those fields by default). Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to erase: hudu_delete_asset_password is the destructive path.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_archive_asset_password",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_asset_passwords_create",
    "field": "name",
    "newValue": "hudu_create_asset_password",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_asset_password",
    "field": "title",
    "newValue": "Create Asset Password",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_asset_password",
    "field": "description",
    "newValue": "Create a Password. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Sensitive: it handles credential material \u2014 never echo a secret value into a log, a description or an error (the SDK redacts those fields by default). Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_asset_password.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_asset_password",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_asset_passwords_delete",
    "field": "name",
    "newValue": "hudu_delete_asset_password",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_asset_password",
    "field": "title",
    "newValue": "Delete Asset Password",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_asset_password",
    "field": "description",
    "newValue": "Delete a Password. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Sensitive: it handles credential material \u2014 never echo a secret value into a log, a description or an error (the SDK redacts those fields by default). Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_asset_password keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_asset_password",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_asset_passwords_findBySlug",
    "field": "name",
    "newValue": "hudu_find_asset_passwords_by_slug",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_asset_passwords_by_slug",
    "field": "title",
    "newValue": "Find Asset Passwords By Slug",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_asset_passwords_by_slug",
    "field": "description",
    "newValue": "Find one password record by its URL slug. Preferred over asset_passwords.list for a slug lookup. Returns one record: a compact AssetPasswordSummary (passwordable_id, passwordable_type, description, \u2026 dropped); expand: true returns the full AssetPassword. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_asset_password for that). Sensitive: returns credential-shaped fields; the SDK redacts them from logs by default.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_asset_passwords_by_slug",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_asset_passwords_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto asset_passwords.resolve, exactly like hudu_get_asset_password (the retained name). hudu_get_asset_password is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_asset_passwords_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto asset_passwords.search, exactly like hudu_search (the retained name). hudu_search performs the same bounded search with body-aware ranking and snippets. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_asset_passwords_resolve",
    "field": "name",
    "newValue": "hudu_get_asset_password",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_asset_password",
    "field": "title",
    "newValue": "Get Asset Password",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_asset_password",
    "field": "description",
    "newValue": "Resolve a password record from an id, name or slug. Use when the identifier is a name or slug; asset_passwords.get returns the full record including the secret. Returns one record: a compact AssetPasswordSummary (passwordable_id, passwordable_type, description, \u2026 dropped); expand: true returns the full AssetPassword. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search. Sensitive: returns credential-shaped fields; the SDK redacts them from logs by default.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_asset_password",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_asset_passwords_search",
    "field": "name",
    "newValue": "hudu_search_asset_passwords",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_search_asset_passwords",
    "field": "title",
    "newValue": "Search Asset Passwords",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_search_asset_passwords",
    "field": "description",
    "newValue": "Search password records by a free-text query. Preferred over asset_passwords.list for any text search. Returns rows: a compact AssetPasswordSummary (passwordable_id, passwordable_type, description, \u2026 dropped); expand: true returns the full AssetPassword. Bounded: limit defaults to 25 and is hard-capped at 100; a larger value throws CONFIG_ERROR and is never silently clamped. Do not use it when you already have an id, slug or domain: call hudu_get_asset_password instead. Sensitive: returns credential-shaped fields; the SDK redacts them from logs by default.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_search_asset_passwords",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_asset_passwords_unarchive",
    "field": "name",
    "newValue": "hudu_unarchive_asset_password",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_unarchive_asset_password",
    "field": "title",
    "newValue": "Unarchive Asset Password",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_unarchive_asset_password",
    "field": "description",
    "newValue": "Unarchive a Password. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Sensitive: it handles credential material \u2014 never echo a secret value into a log, a description or an error (the SDK redacts those fields by default). Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it on a record that is not archived: call hudu_update_asset_password to change fields.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_unarchive_asset_password",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_asset_passwords_update",
    "field": "name",
    "newValue": "hudu_update_asset_password",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_asset_password",
    "field": "title",
    "newValue": "Update Asset Password",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_asset_password",
    "field": "description",
    "newValue": "Update a Password. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Sensitive: it handles credential material \u2014 never echo a secret value into a log, a description or an error (the SDK redacts those fields by default). Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_asset_password.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_asset_password",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_assets_archive",
    "field": "name",
    "newValue": "hudu_archive_asset",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_archive_asset",
    "field": "title",
    "newValue": "Archive Asset",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_archive_asset",
    "field": "description",
    "newValue": "Archive an Asset. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to erase: hudu_delete_asset is the destructive path.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_archive_asset",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_assets_create",
    "field": "name",
    "newValue": "hudu_create_asset",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_asset",
    "field": "title",
    "newValue": "Create Asset",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_asset",
    "field": "description",
    "newValue": "Create an Asset. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_asset.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_asset",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_assets_delete",
    "field": "name",
    "newValue": "hudu_delete_asset",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_asset",
    "field": "title",
    "newValue": "Delete Asset",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_asset",
    "field": "description",
    "newValue": "Delete an Asset. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_asset keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_asset",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_assets_findBySerial",
    "field": "name",
    "newValue": "hudu_find_assets_by_serial",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_assets_by_serial",
    "field": "title",
    "newValue": "Find Assets By Serial",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_assets_by_serial",
    "field": "description",
    "newValue": "Find one asset by its primary serial number. Preferred over assets.listAllAcrossCompanies for any serial lookup. Returns one record: a compact AssetSummary (slug, primary_mail, primary_model, \u2026 dropped); expand: true returns the full Asset. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_asset for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_assets_by_serial",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_assets_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto assets.resolve, exactly like hudu_get_asset (the retained name). hudu_get_asset is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_assets_getContext",
    "field": "name",
    "newValue": "hudu_get_asset_context",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_asset_context",
    "field": "title",
    "newValue": "Get Asset Context",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_asset_context",
    "field": "description",
    "newValue": "Fetch an asset with the context an agent needs about it. Preferred over four separate calls when an agent starts from an asset. Returns AssetContext. Bounded: every sub-list uses limit (default 25, max 100) \u2014 no sub-fetch walks the account. Not for paging: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_asset_context",
    "field": "annotations.bounded",
    "newValue": "default 25 records per sub-list, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_assets_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto assets.search, exactly like hudu_search (the retained name). hudu_search performs the same bounded search with body-aware ranking and snippets. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_assets_listAcrossCompanies",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto assets.search, exactly like hudu_search (the retained name). hudu_search covers the same account-wide bounded search (pass company_id for one company). A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_assets_moveLayout",
    "field": "name",
    "newValue": "hudu_move_layout_assets",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_move_layout_assets",
    "field": "title",
    "newValue": "Move Layout Assets",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_move_layout_assets",
    "field": "description",
    "newValue": "Move asset to a different layout. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff).",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_move_layout_assets",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_assets_resolve",
    "field": "name",
    "newValue": "hudu_get_asset",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_asset",
    "field": "title",
    "newValue": "Get Asset",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_asset",
    "field": "description",
    "newValue": "Resolve an asset from an id, serial, name or slug. Use when the identifier is a serial or a name; assets.get is cheaper for a known id. Returns one record: a compact AssetSummary (slug, primary_mail, primary_model, \u2026 dropped); expand: true returns the full Asset. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_asset",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_assets_search",
    "field": "name",
    "newValue": "hudu_search_assets",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_search_assets",
    "field": "title",
    "newValue": "Search Assets",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_search_assets",
    "field": "description",
    "newValue": "Search assets by a free-text query, optionally scoped to one company. Preferred over the asset list endpoints for any text search. Returns rows: a compact AssetSummary (slug, primary_mail, primary_model, \u2026 dropped); expand: true returns the full Asset. Bounded: limit defaults to 25 and is hard-capped at 100; a larger value throws CONFIG_ERROR and is never silently clamped. Do not use it when you already have an id, slug or domain: call hudu_get_asset instead.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_search_assets",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_assets_unarchive",
    "field": "name",
    "newValue": "hudu_unarchive_asset",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_unarchive_asset",
    "field": "title",
    "newValue": "Unarchive Asset",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_unarchive_asset",
    "field": "description",
    "newValue": "Unarchive an Asset. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it on a record that is not archived: call hudu_update_asset to change fields.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_unarchive_asset",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_assets_update",
    "field": "name",
    "newValue": "hudu_update_asset",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_asset",
    "field": "title",
    "newValue": "Update Asset",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_asset",
    "field": "description",
    "newValue": "Update an Asset. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_asset.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_asset",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_cards_jump",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto cards.resolve, exactly like hudu_get_card (the retained name). hudu_get_card is the retained helper-tier entry point for the same record. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_cards_lookup",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto cards.resolve, exactly like hudu_get_card (the retained name). hudu_get_card is the retained helper-tier entry point for the same record. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_cards_resolve",
    "field": "name",
    "newValue": "hudu_get_card",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_card",
    "field": "title",
    "newValue": "Get Card",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_card",
    "field": "description",
    "newValue": "Resolve an integrator card from an integration identifier. Preferred over calling cards.lookup and cards.jump directly when the goal is 'give me the card for this integration record'. Returns one record: a compact IntegratorCardSummary (data, office_365_assigned_products, exchange_license_assign_date, \u2026 dropped); expand: true returns the full IntegratorCard. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_card",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_companies_archive",
    "field": "name",
    "newValue": "hudu_archive_company",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_archive_company",
    "field": "title",
    "newValue": "Archive Company",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_archive_company",
    "field": "description",
    "newValue": "Archive a specific company. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to erase: hudu_delete_company is the destructive path.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_archive_company",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_companies_create",
    "field": "name",
    "newValue": "hudu_create_company",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_company",
    "field": "title",
    "newValue": "Create Company",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_company",
    "field": "description",
    "newValue": "Create a new company. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_company.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_company",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_companies_delete",
    "field": "name",
    "newValue": "hudu_delete_company",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_company",
    "field": "title",
    "newValue": "Delete Company",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_company",
    "field": "description",
    "newValue": "Delete a specific company. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_company keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_company",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_companies_findByDomain",
    "field": "name",
    "newValue": "hudu_find_companies_by_domain",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_companies_by_domain",
    "field": "title",
    "newValue": "Find Companies By Domain",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_companies_by_domain",
    "field": "description",
    "newValue": "Find one company by its website/domain. Preferred over companies.list for any domain lookup. Returns one record: a compact CompanySummary (address_line_1, address_line_2, zip, \u2026 dropped); expand: true returns the full Company. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_company for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_companies_by_domain",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_companies_findBySlug",
    "field": "name",
    "newValue": "hudu_find_companies_by_slug",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_companies_by_slug",
    "field": "title",
    "newValue": "Find Companies By Slug",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_companies_by_slug",
    "field": "description",
    "newValue": "Find one company by its URL slug. Preferred over companies.list for a slug lookup. Returns one record: a compact CompanySummary (address_line_1, address_line_2, zip, \u2026 dropped); expand: true returns the full Company. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_company for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_companies_by_slug",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_companies_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto companies.resolve, exactly like hudu_get_company (the retained name). hudu_get_company is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_companies_getContext",
    "field": "name",
    "newValue": "hudu_get_company_context",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_company_context",
    "field": "title",
    "newValue": "Get Company Context",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_company_context",
    "field": "description",
    "newValue": "Fetch a company together with the bounded context an agent needs about it. Preferred over four separate list calls when an agent starts work on a company. Returns CompanyContext. Bounded: every sub-list uses limit (default 25, max 100) \u2014 no sub-fetch walks the account. Not for paging: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_company_context",
    "field": "annotations.bounded",
    "newValue": "default 25 records per sub-list, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_companies_jump",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto companies.resolve, exactly like hudu_get_company (the retained name). hudu_get_company is the retained helper-tier entry point for the same record. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_companies_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto companies.search, exactly like hudu_search (the retained name). hudu_search performs the same bounded search with body-aware ranking and snippets. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_companies_resolve",
    "field": "name",
    "newValue": "hudu_get_company",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_company",
    "field": "title",
    "newValue": "Get Company",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_company",
    "field": "description",
    "newValue": "Resolve a company from an id, name, slug or domain. Use when the caller has an identifier rather than a numeric id; companies.get is cheaper when the id is known. Returns one record: a compact CompanySummary (address_line_1, address_line_2, zip, \u2026 dropped); expand: true returns the full Company. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_company",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_companies_search",
    "field": "name",
    "newValue": "hudu_search_companies",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_search_companies",
    "field": "title",
    "newValue": "Search Companies",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_search_companies",
    "field": "description",
    "newValue": "Search companies by a free-text query. Preferred over companies.list for any text search. Returns rows: a compact CompanySummary (address_line_1, address_line_2, zip, \u2026 dropped); expand: true returns the full Company. Bounded: limit defaults to 25 and is hard-capped at 100; a larger value throws CONFIG_ERROR and is never silently clamped. Do not use it when you already have an id, slug or domain: call hudu_get_company instead.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_search_companies",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_companies_unarchive",
    "field": "name",
    "newValue": "hudu_unarchive_company",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_unarchive_company",
    "field": "title",
    "newValue": "Unarchive Company",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_unarchive_company",
    "field": "description",
    "newValue": "Unarchive a specific company. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it on a record that is not archived: call hudu_update_company to change fields.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_unarchive_company",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_companies_update",
    "field": "name",
    "newValue": "hudu_update_company",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_company",
    "field": "title",
    "newValue": "Update Company",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_company",
    "field": "description",
    "newValue": "Update a specific company. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_company.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_company",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_expirations_delete",
    "field": "name",
    "newValue": "hudu_delete_expiration",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_expiration",
    "field": "title",
    "newValue": "Delete Expiration",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_expiration",
    "field": "description",
    "newValue": "Delete an expiration. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_expiration keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_expiration",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_expirations_findByResource",
    "field": "name",
    "newValue": "hudu_find_expirations_by_resource",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_expirations_by_resource",
    "field": "title",
    "newValue": "Find Expirations By Resource",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_expirations_by_resource",
    "field": "description",
    "newValue": "List the expirations attached to one resource. Preferred over expirations.listAll for 'when does this record expire'. Returns one record: a compact ExpirationSummary (account_id, archived_at, created_at, \u2026 dropped); expand: true returns the full Expiration. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_expiration for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_expirations_by_resource",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_expirations_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto expirations.findByResource, exactly like hudu_find_expirations_by_resource (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_find_expirations_by_resource is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_expirations_resolve",
    "field": "name",
    "newValue": "hudu_get_expiration",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_expiration",
    "field": "title",
    "newValue": "Get Expiration",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_expiration",
    "field": "description",
    "newValue": "Resolve an expiration from an id or from its resource. Use when the identifier is a resource pair; expirations.get is cheaper for a known id. Returns one record: a compact ExpirationSummary (account_id, archived_at, created_at, \u2026 dropped); expand: true returns the full Expiration. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_expiration",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_expirations_update",
    "field": "name",
    "newValue": "hudu_update_expiration",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_expiration",
    "field": "title",
    "newValue": "Update Expiration",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_expiration",
    "field": "description",
    "newValue": "Update an expiration. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_expiration.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_expiration",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_flag_types_create",
    "field": "name",
    "newValue": "hudu_create_flag_type",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_flag_type",
    "field": "title",
    "newValue": "Create Flag Type",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_flag_type",
    "field": "description",
    "newValue": "Create a new Flag Type. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_flag_type.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_flag_type",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_flag_types_delete",
    "field": "name",
    "newValue": "hudu_delete_flag_type",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_flag_type",
    "field": "title",
    "newValue": "Delete Flag Type",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_flag_type",
    "field": "description",
    "newValue": "Delete a Flag Type. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_flag_type keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_flag_type",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_flag_types_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto flag_types.resolve, exactly like hudu_get_flag_type (the retained name). hudu_get_flag_type is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_flag_types_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto flag_types.resolve, exactly like hudu_get_flag_type (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_flag_type is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_flag_types_resolve",
    "field": "name",
    "newValue": "hudu_get_flag_type",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_flag_type",
    "field": "title",
    "newValue": "Get Flag Type",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_flag_type",
    "field": "description",
    "newValue": "Resolve a flag type from an id, name or slug. Use when the identifier is a flag-type name; flag_types.get is cheaper for a known id. Returns one record: a compact FlagTypeSummary (created_at, updated_at, \u2026 dropped); expand: true returns the full FlagType. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_flag_type",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_flag_types_update",
    "field": "name",
    "newValue": "hudu_update_flag_type",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_flag_type",
    "field": "title",
    "newValue": "Update Flag Type",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_flag_type",
    "field": "description",
    "newValue": "Update a Flag Type. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_flag_type.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_flag_type",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_flags_create",
    "field": "name",
    "newValue": "hudu_create_flag",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_flag",
    "field": "title",
    "newValue": "Create Flag",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_flag",
    "field": "description",
    "newValue": "Create a new Flag. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_flag.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_flag",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_flags_delete",
    "field": "name",
    "newValue": "hudu_delete_flag",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_flag",
    "field": "title",
    "newValue": "Delete Flag",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_flag",
    "field": "description",
    "newValue": "Delete a Flag. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_flag keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_flag",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_flags_findByFlagable",
    "field": "name",
    "newValue": "hudu_find_flags_by_flagable",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_flags_by_flagable",
    "field": "title",
    "newValue": "Find Flags By Flagable",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_flags_by_flagable",
    "field": "description",
    "newValue": "List the flags attached to one record. Preferred over flags.listAll for the 'is this record flagged' question. Returns one record: a compact FlagSummary (created_at, \u2026 dropped); expand: true returns the full Flag. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_flag for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_flags_by_flagable",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_flags_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto flags.resolve, exactly like hudu_get_flag (the retained name). hudu_get_flag is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_flags_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto flags.findByFlagable, exactly like hudu_find_flags_by_flagable (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_find_flags_by_flagable is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_flags_resolve",
    "field": "name",
    "newValue": "hudu_get_flag",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_flag",
    "field": "title",
    "newValue": "Get Flag",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_flag",
    "field": "description",
    "newValue": "Resolve a flag by its id. Prefer flags.findByFlagable when the flagged record is known; resolve is for a known flag id. Returns one record: a compact FlagSummary (created_at, \u2026 dropped); expand: true returns the full Flag. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_flag",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_flags_update",
    "field": "name",
    "newValue": "hudu_update_flag",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_flag",
    "field": "title",
    "newValue": "Update Flag",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_flag",
    "field": "description",
    "newValue": "Update a Flag. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_flag.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_flag",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_folders_create",
    "field": "name",
    "newValue": "hudu_create_folder",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_folder",
    "field": "title",
    "newValue": "Create Folder",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_folder",
    "field": "description",
    "newValue": "Create a folder. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_folder.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_folder",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_folders_delete",
    "field": "name",
    "newValue": "hudu_delete_folder",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_folder",
    "field": "title",
    "newValue": "Delete Folder",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_folder",
    "field": "description",
    "newValue": "Delete a folder. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_folder keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_folder",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_folders_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto folders.resolve, exactly like hudu_get_folder (the retained name). hudu_get_folder is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_folders_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto folders.resolve, exactly like hudu_get_folder (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_folder is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_folders_resolve",
    "field": "name",
    "newValue": "hudu_get_folder",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_folder",
    "field": "title",
    "newValue": "Get Folder",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_folder",
    "field": "description",
    "newValue": "Resolve a folder from an id or name, optionally scoped to a company. Use when the identifier is a folder name; folders.get is cheaper for a known id. Returns one record: a compact FolderSummary (description, created_at, \u2026 dropped); expand: true returns the full Folder. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_folder",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_folders_update",
    "field": "name",
    "newValue": "hudu_update_folder",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_folder",
    "field": "title",
    "newValue": "Update Folder",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_folder",
    "field": "description",
    "newValue": "Update a folder. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_folder.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_folder",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_groups_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto groups.resolve, exactly like hudu_get_group (the retained name). hudu_get_group is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_groups_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto groups.search, exactly like hudu_search (the retained name). hudu_search performs the same bounded search with body-aware ranking and snippets. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_groups_resolve",
    "field": "name",
    "newValue": "hudu_get_group",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_group",
    "field": "title",
    "newValue": "Get Group",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_group",
    "field": "description",
    "newValue": "Resolve a user group from an id, name or slug. Use when the identifier is a group name; groups.get is cheaper for a known id. Returns one record: a compact GroupSummary (url, created_at, members, \u2026 dropped); expand: true returns the full Group. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_group",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_groups_search",
    "field": "name",
    "newValue": "hudu_search_groups",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_search_groups",
    "field": "title",
    "newValue": "Search Groups",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_search_groups",
    "field": "description",
    "newValue": "Search groups by name. Preferred over groups.list for any group-name search. Returns rows: a compact GroupSummary (url, created_at, members, \u2026 dropped); expand: true returns the full Group. Bounded: limit defaults to 25 and is hard-capped at 100; a larger value throws CONFIG_ERROR and is never silently clamped. Do not use it when you already have an id, slug or domain: call hudu_get_group instead.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_search_groups",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_ip_addresses_create",
    "field": "name",
    "newValue": "hudu_create_ip_address",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_ip_address",
    "field": "title",
    "newValue": "Create IP Address",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_ip_address",
    "field": "description",
    "newValue": "Create a new IP address. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_ip_address.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_ip_address",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_ip_addresses_delete",
    "field": "name",
    "newValue": "hudu_delete_ip_address",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_ip_address",
    "field": "title",
    "newValue": "Delete IP Address",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_ip_address",
    "field": "description",
    "newValue": "Delete an IP address. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_ip_address keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_ip_address",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_ip_addresses_findByAddress",
    "field": "name",
    "newValue": "hudu_find_ip_addresses_by_address",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_ip_addresses_by_address",
    "field": "title",
    "newValue": "Find IP Addresses By Address",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_ip_addresses_by_address",
    "field": "description",
    "newValue": "Find one IP address record by its address. Preferred over ip_addresses.listAll for any address lookup. Returns one record: a compact IpAddressSummary (notes, skip_dns_validation, \u2026 dropped); expand: true returns the full IpAddress. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_ip_address for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_ip_addresses_by_address",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_ip_addresses_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto ip_addresses.resolve, exactly like hudu_get_ip_address (the retained name). hudu_get_ip_address is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_ip_addresses_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto ip_addresses.resolve, exactly like hudu_get_ip_address (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_ip_address is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_ip_addresses_resolve",
    "field": "name",
    "newValue": "hudu_get_ip_address",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_ip_address",
    "field": "title",
    "newValue": "Get IP Address",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_ip_address",
    "field": "description",
    "newValue": "Resolve an IP address record from an id, the address, or its FQDN. Use when the identifier is a CIDR address or an FQDN; ip_addresses.get is cheaper for a known id. Returns one record: a compact IpAddressSummary (notes, skip_dns_validation, \u2026 dropped); expand: true returns the full IpAddress. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_ip_address",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_ip_addresses_update",
    "field": "name",
    "newValue": "hudu_update_ip_address",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_ip_address",
    "field": "title",
    "newValue": "Update IP Address",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_ip_address",
    "field": "description",
    "newValue": "Update an existing IP address. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_ip_address.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_ip_address",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_label_types_create",
    "field": "name",
    "newValue": "hudu_create_label_type",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_label_type",
    "field": "title",
    "newValue": "Create Label Type",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_label_type",
    "field": "description",
    "newValue": "Create a new Label Type. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_label_type.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_label_type",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_label_types_delete",
    "field": "name",
    "newValue": "hudu_delete_label_type",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_label_type",
    "field": "title",
    "newValue": "Delete Label Type",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_label_type",
    "field": "description",
    "newValue": "Delete a Label Type. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_label_type keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_label_type",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_label_types_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto label_types.resolve, exactly like hudu_get_label_type (the retained name). hudu_get_label_type is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_label_types_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto label_types.resolve, exactly like hudu_get_label_type (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_label_type is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_label_types_resolve",
    "field": "name",
    "newValue": "hudu_get_label_type",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_label_type",
    "field": "title",
    "newValue": "Get Label Type",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_label_type",
    "field": "description",
    "newValue": "Resolve a label type from an id, slug or exact name. Use when the identifier is a label-type name; label_types.get is cheaper for a known id. Returns one record: a compact LabelTypeSummary (allowed_company_ids, created_at, updated_at, \u2026 dropped); expand: true returns the full LabelType. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_label_type",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_label_types_update",
    "field": "name",
    "newValue": "hudu_update_label_type",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_label_type",
    "field": "title",
    "newValue": "Update Label Type",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_label_type",
    "field": "description",
    "newValue": "Update a Label Type. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_label_type.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_label_type",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_labels_create",
    "field": "name",
    "newValue": "hudu_create_label",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_label",
    "field": "title",
    "newValue": "Create Label",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_label",
    "field": "description",
    "newValue": "Create a new Label. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_label.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_label",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_labels_delete",
    "field": "name",
    "newValue": "hudu_delete_label",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_label",
    "field": "title",
    "newValue": "Delete Label",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_label",
    "field": "description",
    "newValue": "Delete a Label. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_label keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_label",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_labels_findByLabelable",
    "field": "name",
    "newValue": "hudu_find_labels_by_labelable",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_labels_by_labelable",
    "field": "title",
    "newValue": "Find Labels By Labelable",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_labels_by_labelable",
    "field": "description",
    "newValue": "List the labels attached to one record. Preferred over labels.listAll for 'what is this record tagged with'. Returns one record: a compact LabelSummary (created_at, \u2026 dropped); expand: true returns the full Label. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_label for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_labels_by_labelable",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_labels_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto labels.resolve, exactly like hudu_get_label (the retained name). hudu_get_label is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_labels_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto labels.findByLabelable, exactly like hudu_find_labels_by_labelable (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_find_labels_by_labelable is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_labels_resolve",
    "field": "name",
    "newValue": "hudu_get_label",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_label",
    "field": "title",
    "newValue": "Get Label",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_label",
    "field": "description",
    "newValue": "Resolve a label by its id or by the record it is attached to. Use labels.findByLabelable for the list form; resolve is for a single known label. Returns one record: a compact LabelSummary (created_at, \u2026 dropped); expand: true returns the full Label. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_label",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_labels_update",
    "field": "name",
    "newValue": "hudu_update_label",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_label",
    "field": "title",
    "newValue": "Update Label",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_label",
    "field": "description",
    "newValue": "Update a Label. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_label.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_label",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_lists_create",
    "field": "name",
    "newValue": "hudu_create_list",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_list",
    "field": "title",
    "newValue": "Create List",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_list",
    "field": "description",
    "newValue": "Create a new List. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_list.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_list",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_lists_delete",
    "field": "name",
    "newValue": "hudu_delete_list",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_list",
    "field": "title",
    "newValue": "Delete List",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_list",
    "field": "description",
    "newValue": "Delete a List. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_list keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_list",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_lists_findByName",
    "field": "name",
    "newValue": "hudu_find_lists_by_name",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_lists_by_name",
    "field": "title",
    "newValue": "Find Lists By Name",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_lists_by_name",
    "field": "description",
    "newValue": "Find a list by its exact name. Preferred over lists.listAll for a name lookup. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_list for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_lists_by_name",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_lists_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto lists.resolve, exactly like hudu_get_list (the retained name). hudu_get_list is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_lists_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto lists.findByName, exactly like hudu_find_lists_by_name (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_find_lists_by_name is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_lists_resolve",
    "field": "name",
    "newValue": "hudu_get_list",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_list",
    "field": "title",
    "newValue": "Get List",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_list",
    "field": "description",
    "newValue": "Resolve a list from an id or its exact name. Use when the identifier is a list name; lists.get is cheaper for a known id. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_list",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_lists_update",
    "field": "name",
    "newValue": "hudu_update_list",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_list",
    "field": "title",
    "newValue": "Update List",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_list",
    "field": "description",
    "newValue": "Update a List. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_list.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_list",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_magic_dash_create",
    "field": "name",
    "newValue": "hudu_create_magic_dash",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_magic_dash",
    "field": "title",
    "newValue": "Create Magic Dash",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_magic_dash",
    "field": "description",
    "newValue": "Create or update a Magic Dash Item. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_magic_dash.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_magic_dash",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_magic_dash_delete",
    "field": "name",
    "newValue": "hudu_delete_magic_dash",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_magic_dash",
    "field": "title",
    "newValue": "Delete Magic Dash",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_magic_dash",
    "field": "description",
    "newValue": "Delete a Magic Dash Item without an ID. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_magic_dash keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_magic_dash",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_magic_dash_deleteById",
    "field": "name",
    "newValue": "hudu_delete_magic_dash_by_id",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_magic_dash_by_id",
    "field": "title",
    "newValue": "Delete Magic Dash By ID",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_magic_dash_by_id",
    "field": "description",
    "newValue": "Delete a Magic Dash item with the specified ID. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: BULK by title \u2014 deletes every Magic Dash item carrying that title for the company. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff).",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_magic_dash_by_id",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_magic_dash_findByCompany",
    "field": "name",
    "newValue": "hudu_find_magic_dash_by_company",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_magic_dash_by_company",
    "field": "title",
    "newValue": "Find Magic Dash By Company",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_magic_dash_by_company",
    "field": "description",
    "newValue": "List the Magic Dash items of one company. Preferred over magic_dash.listAll for the per-company dashboard read. Returns one record: a compact MagicDashSummary (content_link, content, \u2026 dropped); expand: true returns the full MagicDash. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_magic_dash for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_magic_dash_by_company",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_magic_dash_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto magic_dash.findByCompany, exactly like hudu_find_magic_dash_by_company (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_find_magic_dash_by_company is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_magic_dash_resolve",
    "field": "name",
    "newValue": "hudu_get_magic_dash",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_magic_dash",
    "field": "title",
    "newValue": "Get Magic Dash",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_magic_dash",
    "field": "description",
    "newValue": "Resolve a Magic Dash item from an id or a title. Use when the identifier is a dashboard title; magic_dash.findByCompany is the company-scoped path. Returns one record: a compact MagicDashSummary (content_link, content, \u2026 dropped); expand: true returns the full MagicDash. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_magic_dash",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_magic_dash_updatePositions",
    "field": "name",
    "newValue": "hudu_update_magic_dash_positions",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_magic_dash_positions",
    "field": "title",
    "newValue": "Update Magic Dash Positions",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_magic_dash_positions",
    "field": "description",
    "newValue": "Update positions of multiple Magic Dash Items. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: BULK \u2014 rewrites the position of every item in the submitted array in one call. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff).",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_magic_dash_positions",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_matchers_delete",
    "field": "name",
    "newValue": "hudu_delete_matcher",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_matcher",
    "field": "title",
    "newValue": "Delete Matcher",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_matcher",
    "field": "description",
    "newValue": "Delete a Matcher. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_matcher keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_matcher",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_matchers_findBySyncId",
    "field": "name",
    "newValue": "hudu_find_matchers_by_sync_id",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_matchers_by_sync_id",
    "field": "title",
    "newValue": "Find Matchers By Sync ID",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_matchers_by_sync_id",
    "field": "description",
    "newValue": "Find the matcher for an external sync id. Preferred over matchers.listAll for any sync-id lookup. Returns array. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_matcher for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_matchers_by_sync_id",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_matchers_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto matchers.resolve, exactly like hudu_get_matcher (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_matcher is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_matchers_resolve",
    "field": "name",
    "newValue": "hudu_get_matcher",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_matcher",
    "field": "title",
    "newValue": "Get Matcher",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_matcher",
    "field": "description",
    "newValue": "Resolve a matcher from an id, a sync_id or an integration identifier. Use when correlating a Hudu record with an external system; matchers.listAll is the enumeration path. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_matcher",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_matchers_update",
    "field": "name",
    "newValue": "hudu_update_matcher",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_matcher",
    "field": "title",
    "newValue": "Update Matcher",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_matcher",
    "field": "description",
    "newValue": "Update a Matcher. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_matcher.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_matcher",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_networks_create",
    "field": "name",
    "newValue": "hudu_create_network",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_network",
    "field": "title",
    "newValue": "Create Network",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_network",
    "field": "description",
    "newValue": "Create a new network. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_network.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_network",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_networks_delete",
    "field": "name",
    "newValue": "hudu_delete_network",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_network",
    "field": "title",
    "newValue": "Delete Network",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_network",
    "field": "description",
    "newValue": "Delete a network. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_network keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_network",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_networks_findByAddress",
    "field": "name",
    "newValue": "hudu_find_networks_by_address",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_networks_by_address",
    "field": "title",
    "newValue": "Find Networks By Address",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_networks_by_address",
    "field": "description",
    "newValue": "Find one network by its CIDR address. Preferred over networks.list for any CIDR lookup. Returns one record: a compact NetworkSummary (description, notes, ancestry, \u2026 dropped); expand: true returns the full Network. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_network for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_networks_by_address",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_networks_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto networks.resolve, exactly like hudu_get_network (the retained name). hudu_get_network is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_networks_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto networks.resolve, exactly like hudu_get_network (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_network is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_networks_resolve",
    "field": "name",
    "newValue": "hudu_get_network",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_network",
    "field": "title",
    "newValue": "Get Network",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_network",
    "field": "description",
    "newValue": "Resolve a network from an id, name, slug or address. Use when the identifier is a name, slug or CIDR; networks.get is cheaper for a known id. Returns one record: a compact NetworkSummary (description, notes, ancestry, \u2026 dropped); expand: true returns the full Network. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_network",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_networks_update",
    "field": "name",
    "newValue": "hudu_update_network",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_network",
    "field": "title",
    "newValue": "Update Network",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_network",
    "field": "description",
    "newValue": "Update an existing network. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_network.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_network",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_operations_resolveAny",
    "field": "name",
    "newValue": "hudu_resolve_any",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_resolve_any",
    "field": "title",
    "newValue": "Resolve Any",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_resolve_any",
    "field": "description",
    "newValue": "Resolve an identifier against several resources and return the candidates that match. Preferred when the caller has an identifier but not the resource type; use the resource's own resolve when the type is known. Returns ResolutionCandidateHit rows for each resource that matched, with scanned/truncated counts; it does not throw RESOLUTION_AMBIGUOUS across resources. Bounded: limit defaults to 25 and is hard-capped at 100; a larger value throws CONFIG_ERROR and is never silently clamped. Do not use it when the resource type is known: call that resource's hudu_get_<singular>.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_resolve_any",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_operations_searchAcrossResources",
    "field": "name",
    "newValue": "hudu_search_across_resources",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_search_across_resources",
    "field": "title",
    "newValue": "Search Across Resources",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_search_across_resources",
    "field": "description",
    "newValue": "Search several Hudu resources for one query in a single bounded call. Preferred over calling several resources' search methods yourself whenever the caller does not know which resource holds the record. Returns one SearchHit row per match as { resource, id, label, item }, in resource order (expand: true returns the full typed records). Bounded: limit defaults to 25 and is hard-capped at 100; a larger value throws CONFIG_ERROR and is never silently clamped. Do not use it when the resource type is known: that resource's own hudu_search_<resource> issues one request instead of eight.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_search_across_resources",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_password_folders_create",
    "field": "name",
    "newValue": "hudu_create_password_folder",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_password_folder",
    "field": "title",
    "newValue": "Create Password Folder",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_password_folder",
    "field": "description",
    "newValue": "Create a new password folder. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Sensitive: it handles credential material \u2014 never echo a secret value into a log, a description or an error (the SDK redacts those fields by default). Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_password_folder.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_password_folder",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_password_folders_delete",
    "field": "name",
    "newValue": "hudu_delete_password_folder",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_password_folder",
    "field": "title",
    "newValue": "Delete Password Folder",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_password_folder",
    "field": "description",
    "newValue": "Delete a password folder. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Sensitive: it handles credential material \u2014 never echo a secret value into a log, a description or an error (the SDK redacts those fields by default). Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_password_folder keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_password_folder",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_password_folders_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto password_folders.resolve, exactly like hudu_get_password_folder (the retained name). hudu_get_password_folder is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_password_folders_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto password_folders.search, exactly like hudu_search (the retained name). hudu_search performs the same bounded search with body-aware ranking and snippets. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_password_folders_resolve",
    "field": "name",
    "newValue": "hudu_get_password_folder",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_password_folder",
    "field": "title",
    "newValue": "Get Password Folder",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_password_folder",
    "field": "description",
    "newValue": "Resolve a password folder from an id or name, optionally scoped to a company. Use when the identifier is a folder name; password_folders.get is cheaper for a known id. Returns one record: a compact PasswordFolderSummary (description, allowed_groups, created_at, \u2026 dropped); expand: true returns the full PasswordFolder. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search. Sensitive: returns credential-shaped fields; the SDK redacts them from logs by default.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_password_folder",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_password_folders_search",
    "field": "name",
    "newValue": "hudu_search_password_folders",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_search_password_folders",
    "field": "title",
    "newValue": "Search Password Folders",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_search_password_folders",
    "field": "description",
    "newValue": "Search password folders by a free-text query. Preferred over password_folders.list for any text search. Returns rows: a compact PasswordFolderSummary (description, allowed_groups, created_at, \u2026 dropped); expand: true returns the full PasswordFolder. Bounded: limit defaults to 25 and is hard-capped at 100; a larger value throws CONFIG_ERROR and is never silently clamped. Do not use it when you already have an id, slug or domain: call hudu_get_password_folder instead. Sensitive: returns credential-shaped fields; the SDK redacts them from logs by default.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_search_password_folders",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_password_folders_update",
    "field": "name",
    "newValue": "hudu_update_password_folder",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_password_folder",
    "field": "title",
    "newValue": "Update Password Folder",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_password_folder",
    "field": "description",
    "newValue": "Update an existing password folder. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Sensitive: it handles credential material \u2014 never echo a secret value into a log, a description or an error (the SDK redacts those fields by default). Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_password_folder.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_password_folder",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_procedure_tasks_create",
    "field": "name",
    "newValue": "hudu_create_procedure_task",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_procedure_task",
    "field": "title",
    "newValue": "Create Procedure Task",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_procedure_task",
    "field": "description",
    "newValue": "Create a new Task on a Process. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_procedure_task.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_procedure_task",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_procedure_tasks_delete",
    "field": "name",
    "newValue": "hudu_delete_procedure_task",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_procedure_task",
    "field": "title",
    "newValue": "Delete Procedure Task",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_procedure_task",
    "field": "description",
    "newValue": "Delete a Task from a Process. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_procedure_task keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_procedure_task",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_procedure_tasks_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto procedure_tasks.resolve, exactly like hudu_get_procedure_task (the retained name). hudu_get_procedure_task is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_procedure_tasks_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto procedure_tasks.resolve, exactly like hudu_get_procedure_task (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_procedure_task is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_procedure_tasks_resolve",
    "field": "name",
    "newValue": "hudu_get_procedure_task",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_procedure_task",
    "field": "title",
    "newValue": "Get Procedure Task",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_procedure_task",
    "field": "description",
    "newValue": "Resolve a procedure task from an id or an exact name inside a procedure. Use when the identifier is a task name inside a known procedure; procedure_tasks.get is cheaper for a known id. Returns one record: a compact ProcedureTaskSummary (description, completion_notes, formatted_due_date, \u2026 dropped); expand: true returns the full ProcedureTask. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_procedure_task",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_procedure_tasks_update",
    "field": "name",
    "newValue": "hudu_update_procedure_task",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_procedure_task",
    "field": "title",
    "newValue": "Update Procedure Task",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_procedure_task",
    "field": "description",
    "newValue": "Update a Task. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_procedure_task.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_procedure_task",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_procedures_create",
    "field": "name",
    "newValue": "hudu_create_procedure",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_procedure",
    "field": "title",
    "newValue": "Create Procedure",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_procedure",
    "field": "description",
    "newValue": "Create a new Process (Template). Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_procedure.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_procedure",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_procedures_createFromTemplate",
    "field": "name",
    "newValue": "hudu_create_procedure_from_template",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_procedure_from_template",
    "field": "title",
    "newValue": "Create Procedure From Template",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_procedure_from_template",
    "field": "description",
    "newValue": "Create a Process from a Global Template. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff).",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_procedure_from_template",
    "field": "inputSchema.options",
    "newValue": {
      "name": "options",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_procedures_delete",
    "field": "name",
    "newValue": "hudu_delete_procedure",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_procedure",
    "field": "title",
    "newValue": "Delete Procedure",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_procedure",
    "field": "description",
    "newValue": "Delete a Process or Run. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_procedure keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_procedure",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_procedures_duplicate",
    "field": "name",
    "newValue": "hudu_duplicate_procedure",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_duplicate_procedure",
    "field": "title",
    "newValue": "Duplicate Procedure",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_duplicate_procedure",
    "field": "description",
    "newValue": "Duplicate an existing Process. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff).",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_duplicate_procedure",
    "field": "inputSchema.options",
    "newValue": {
      "name": "options",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_procedures_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto procedures.resolve, exactly like hudu_get_procedure (the retained name). hudu_get_procedure is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_procedures_getWithTasks",
    "field": "name",
    "newValue": "hudu_get_procedure_with_tasks",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_procedure_with_tasks",
    "field": "title",
    "newValue": "Get Procedure With Tasks",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_procedure_with_tasks",
    "field": "description",
    "newValue": "Fetch a procedure together with its tasks in one call. Preferred over calling procedures.get and procedure_tasks.listAll yourself whenever the goal is to read or report on a process. Returns one record: a compact ProcedureWithTasks (limit, \u2026 dropped); expand: true returns the full ProcedureWithTasksFull. Bounded: every sub-list is fetched with limit, default 25, hard maximum 100 \u2014 no sub-fetch walks the account. Do not use it to page every procedure: it fetches one procedure plus its bounded task list.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_procedure_with_tasks",
    "field": "annotations.bounded",
    "newValue": "default 25 records per sub-list, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_procedures_kickoff",
    "field": "name",
    "newValue": "hudu_kickoff_procedure",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_kickoff_procedure",
    "field": "title",
    "newValue": "Kickoff Procedure",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_kickoff_procedure",
    "field": "description",
    "newValue": "Create a Run from a Process. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff).",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_kickoff_procedure",
    "field": "inputSchema.options",
    "newValue": {
      "name": "options",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_procedures_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto procedures.resolve, exactly like hudu_get_procedure (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_procedure is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_procedures_resolve",
    "field": "name",
    "newValue": "hudu_get_procedure",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_procedure",
    "field": "title",
    "newValue": "Get Procedure",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_procedure",
    "field": "description",
    "newValue": "Resolve a procedure/process from an id, slug or exact name. Use when the identifier is a name or slug; procedures.get is cheaper for a known id. Returns one record: a compact ProcedureSummary (description, object_type, created_at, \u2026 dropped); expand: true returns the full Procedure. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_procedure",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_procedures_update",
    "field": "name",
    "newValue": "hudu_update_procedure",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_procedure",
    "field": "title",
    "newValue": "Update Procedure",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_procedure",
    "field": "description",
    "newValue": "Update an existing Process or Run. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_procedure.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_procedure",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_rack_storage_items_create",
    "field": "name",
    "newValue": "hudu_create_rack_storage_item",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_rack_storage_item",
    "field": "title",
    "newValue": "Create Rack Storage Item",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_rack_storage_item",
    "field": "description",
    "newValue": "Create a Rack Storage Item. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_rack_storage_item.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_rack_storage_item",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_rack_storage_items_delete",
    "field": "name",
    "newValue": "hudu_delete_rack_storage_item",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_rack_storage_item",
    "field": "title",
    "newValue": "Delete Rack Storage Item",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_rack_storage_item",
    "field": "description",
    "newValue": "Delete a Rack Storage Item. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_rack_storage_item keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_rack_storage_item",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_rack_storage_items_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto rack_storage_items.resolve, exactly like hudu_get_rack_storage_item (the retained name). hudu_get_rack_storage_item is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_rack_storage_items_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto rack_storage_items.resolve, exactly like hudu_get_rack_storage_item (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_rack_storage_item is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_rack_storage_items_resolve",
    "field": "name",
    "newValue": "hudu_get_rack_storage_item",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_rack_storage_item",
    "field": "title",
    "newValue": "Get Rack Storage Item",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_rack_storage_item",
    "field": "description",
    "newValue": "Resolve a rack storage item from an id. Use when the item id is known; rack_storage_items.listAll is the enumeration path. Returns one record: a compact RackStorageItemSummary (max_wattage, power_draw, reserved_message, \u2026 dropped); expand: true returns the full RackStorageItem. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_rack_storage_item",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_rack_storage_items_update",
    "field": "name",
    "newValue": "hudu_update_rack_storage_item",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_rack_storage_item",
    "field": "title",
    "newValue": "Update Rack Storage Item",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_rack_storage_item",
    "field": "description",
    "newValue": "Update a Rack Storage Item. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_rack_storage_item.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_rack_storage_item",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_rack_storages_create",
    "field": "name",
    "newValue": "hudu_create_rack_storage",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_rack_storage",
    "field": "title",
    "newValue": "Create Rack Storage",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_rack_storage",
    "field": "description",
    "newValue": "Create a new Rack Storage. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_rack_storage.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_rack_storage",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_rack_storages_delete",
    "field": "name",
    "newValue": "hudu_delete_rack_storage",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_rack_storage",
    "field": "title",
    "newValue": "Delete Rack Storage",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_rack_storage",
    "field": "description",
    "newValue": "Delete a Rack Storage. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_rack_storage keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_rack_storage",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_rack_storages_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto rack_storages.resolve, exactly like hudu_get_rack_storage (the retained name). hudu_get_rack_storage is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_rack_storages_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto rack_storages.resolve, exactly like hudu_get_rack_storage (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_rack_storage is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_rack_storages_resolve",
    "field": "name",
    "newValue": "hudu_get_rack_storage",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_rack_storage",
    "field": "title",
    "newValue": "Get Rack Storage",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_rack_storage",
    "field": "description",
    "newValue": "Resolve a rack storage from an id or name. Use when the identifier is a rack name; rack_storages.get is cheaper for a known id. Returns one record: a compact RackStorageSummary (description, created_at, discarded_at, \u2026 dropped); expand: true returns the full RackStorage. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_rack_storage",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_rack_storages_update",
    "field": "name",
    "newValue": "hudu_update_rack_storage",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_rack_storage",
    "field": "title",
    "newValue": "Update Rack Storage",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_rack_storage",
    "field": "description",
    "newValue": "Update a Rack Storage. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_rack_storage.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_rack_storage",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_relations_create",
    "field": "name",
    "newValue": "hudu_create_relation",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_relation",
    "field": "title",
    "newValue": "Create Relation",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_relation",
    "field": "description",
    "newValue": "Create a Relation. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_relation.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_relation",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_relations_delete",
    "field": "name",
    "newValue": "hudu_delete_relation",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_relation",
    "field": "title",
    "newValue": "Delete Relation",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_relation",
    "field": "description",
    "newValue": "Delete a Relation. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_relation keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_relation",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_relations_findByEndpoints",
    "field": "name",
    "newValue": "hudu_find_relations_by_endpoints",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_relations_by_endpoints",
    "field": "title",
    "newValue": "Find Relations By Endpoints",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_relations_by_endpoints",
    "field": "description",
    "newValue": "List the relations that connect two records. Preferred over relations.listAll for the 'what is this record related to' question. Returns one record: a compact RelationSummary (created_at, updated_at, \u2026 dropped); expand: true returns the full Relation. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_relation for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_relations_by_endpoints",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_relations_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto relations.findByEndpoints, exactly like hudu_find_relations_by_endpoints (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_find_relations_by_endpoints is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_relations_resolve",
    "field": "name",
    "newValue": "hudu_get_relation",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_relation",
    "field": "title",
    "newValue": "Get Relation",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_relation",
    "field": "description",
    "newValue": "Resolve a relation by its id. Prefer relations.findByEndpoints whenever the endpoints are known; resolve is only for a known relation id. Returns one record: a compact RelationSummary (created_at, updated_at, \u2026 dropped); expand: true returns the full Relation. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_relation",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_users_findByEmail",
    "field": "name",
    "newValue": "hudu_find_users_by_email",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_users_by_email",
    "field": "title",
    "newValue": "Find Users By Email",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_users_by_email",
    "field": "description",
    "newValue": "Find one user by their exact email address. Preferred over users.listAll for any email lookup. Returns one record: a compact UserSummary (otp_required_for_login, phone_number, time_zone, \u2026 dropped); expand: true returns the full User. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_user for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_users_by_email",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_users_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto users.resolve, exactly like hudu_get_user (the retained name). hudu_get_user is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_users_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto users.search, exactly like hudu_search (the retained name). hudu_search performs the same bounded search with body-aware ranking and snippets. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_users_resolve",
    "field": "name",
    "newValue": "hudu_get_user",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_user",
    "field": "title",
    "newValue": "Get User",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_user",
    "field": "description",
    "newValue": "Resolve a user from an id, email, slug or exact name. Use when the identifier is an email or a name; users.get is cheaper for a known id. Returns one record: a compact UserSummary (otp_required_for_login, phone_number, time_zone, \u2026 dropped); expand: true returns the full User. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_user",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_users_search",
    "field": "name",
    "newValue": "hudu_search_users",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_search_users",
    "field": "title",
    "newValue": "Search Users",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_search_users",
    "field": "description",
    "newValue": "Search users by a free-text query. Preferred over users.listAll for any text search. Returns rows: a compact UserSummary (otp_required_for_login, phone_number, time_zone, \u2026 dropped); expand: true returns the full User. Bounded: limit defaults to 25 and is hard-capped at 100; a larger value throws CONFIG_ERROR and is never silently clamped. Do not use it when you already have an id, slug or domain: call hudu_get_user instead.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_search_users",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_vlan_zones_create",
    "field": "name",
    "newValue": "hudu_create_vlan_zone",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_vlan_zone",
    "field": "title",
    "newValue": "Create VLAN Zone",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_vlan_zone",
    "field": "description",
    "newValue": "Create a VLAN Zone. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_vlan_zone.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_vlan_zone",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_vlan_zones_delete",
    "field": "name",
    "newValue": "hudu_delete_vlan_zone",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_vlan_zone",
    "field": "title",
    "newValue": "Delete VLAN Zone",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_vlan_zone",
    "field": "description",
    "newValue": "Delete a VLAN Zone. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_vlan_zone keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_vlan_zone",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_vlan_zones_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto vlan_zones.resolve, exactly like hudu_get_vlan_zone (the retained name). hudu_get_vlan_zone is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_vlan_zones_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto vlan_zones.resolve, exactly like hudu_get_vlan_zone (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_vlan_zone is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_vlan_zones_resolve",
    "field": "name",
    "newValue": "hudu_get_vlan_zone",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_vlan_zone",
    "field": "title",
    "newValue": "Get VLAN Zone",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_vlan_zone",
    "field": "description",
    "newValue": "Resolve a VLAN zone from an id, name or slug. Use when the identifier is a name or slug; vlan_zones.get is cheaper for a known id. Returns one record: a compact VlanZoneSummary (description, archived_at, created_at, \u2026 dropped); expand: true returns the full VlanZone. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_vlan_zone",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_vlan_zones_update",
    "field": "name",
    "newValue": "hudu_update_vlan_zone",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_vlan_zone",
    "field": "title",
    "newValue": "Update VLAN Zone",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_vlan_zone",
    "field": "description",
    "newValue": "Update / archive / unarchive a VLAN Zone. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_vlan_zone.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_vlan_zone",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_vlans_create",
    "field": "name",
    "newValue": "hudu_create_vlan",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_vlan",
    "field": "title",
    "newValue": "Create VLAN",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_vlan",
    "field": "description",
    "newValue": "Create a new VLAN. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_vlan.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_vlan",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_vlans_delete",
    "field": "name",
    "newValue": "hudu_delete_vlan",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_vlan",
    "field": "title",
    "newValue": "Delete VLAN",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_vlan",
    "field": "description",
    "newValue": "Delete a VLAN. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_vlan keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_vlan",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_vlans_findByVlanId",
    "field": "name",
    "newValue": "hudu_find_vlans_by_vlan_id",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_vlans_by_vlan_id",
    "field": "title",
    "newValue": "Find Vlans By VLAN ID",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_vlans_by_vlan_id",
    "field": "description",
    "newValue": "Find one VLAN by its vendor VLAN number. Preferred over vlans.list for a VLAN-number lookup. Returns one record: a compact VlanSummary (description, notes, archived_at, \u2026 dropped); expand: true returns the full Vlan. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_vlan for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_vlans_by_vlan_id",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_vlans_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto vlans.resolve, exactly like hudu_get_vlan (the retained name). hudu_get_vlan is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_vlans_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto vlans.resolve, exactly like hudu_get_vlan (the retained name). This resource has no multi-record helper, so both tools resolve ONE record: hudu_get_vlan is the retained read path. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_vlans_resolve",
    "field": "name",
    "newValue": "hudu_get_vlan",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_vlan",
    "field": "title",
    "newValue": "Get VLAN",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_vlan",
    "field": "description",
    "newValue": "Resolve a VLAN from an id, name or VLAN number. Use when the identifier is a name or VLAN number; vlans.get is cheaper for a known id. Returns one record: a compact VlanSummary (description, notes, archived_at, \u2026 dropped); expand: true returns the full Vlan. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_vlan",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_vlans_update",
    "field": "name",
    "newValue": "hudu_update_vlan",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_vlan",
    "field": "title",
    "newValue": "Update VLAN",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_vlan",
    "field": "description",
    "newValue": "Update / archive / unarchive a VLAN. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_vlan.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_vlan",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_websites_create",
    "field": "name",
    "newValue": "hudu_create_website",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_create_website",
    "field": "title",
    "newValue": "Create Website",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_create_website",
    "field": "description",
    "newValue": "Create a Website. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to change an existing record: call hudu_update_website.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_create_website",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_websites_delete",
    "field": "name",
    "newValue": "hudu_delete_website",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_delete_website",
    "field": "title",
    "newValue": "Delete Website",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_delete_website",
    "field": "description",
    "newValue": "Delete a Website. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record, permanently; there is no undo. Irreversible \u2014 dry-run first and prefer archive where the vendor offers it. requiresApproval: the MCP gateway must obtain explicit human approval before this tool runs; it is not safe to call autonomously. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to hide a record you may still need: hudu_archive_website keeps it.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_delete_website",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": []
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate. The registry does not advertise `expectedUpdatedAt` on this operation (the SDK rejects it outside an update), so the replacement carries no fields: the override exists only to drop the duplicated `dryRun`."
  },
  {
    "tool": "hudu_websites_findBySlug",
    "field": "name",
    "newValue": "hudu_find_websites_by_slug",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_find_websites_by_slug",
    "field": "title",
    "newValue": "Find Websites By Slug",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_find_websites_by_slug",
    "field": "description",
    "newValue": "Find one website by its URL slug. Preferred over websites.list for a slug lookup. Returns one record: a compact WebsiteSummary (code, message, keyword, \u2026 dropped); expand: true returns the full Website. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Do not use it to enumerate or to page: it returns at most one exact match (call hudu_search or hudu_get_website for that).",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_find_websites_by_slug",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_websites_get",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto websites.resolve, exactly like hudu_get_website (the retained name). hudu_get_website is the identifier form (id, name, slug, domain); the id-only alias adds nothing. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_websites_list",
    "field": "exclude",
    "newValue": true,
    "reason": "Duplicate outcome: after re-pointing it backs onto websites.search, exactly like hudu_search (the retained name). hudu_search performs the same bounded search with body-aware ranking and snippets. A tools/list where two tools do the same job is worse than a slightly shorter list."
  },
  {
    "tool": "hudu_websites_resolve",
    "field": "name",
    "newValue": "hudu_get_website",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_get_website",
    "field": "title",
    "newValue": "Get Website",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_get_website",
    "field": "description",
    "newValue": "Resolve a website from an id, name or slug. Use when the identifier is a name or slug; websites.get is cheaper for a known id. Returns one record: a compact WebsiteSummary (code, message, keyword, \u2026 dropped); expand: true returns the full Website. Bounded: limit default 25, max 100; the client scan stops at 500 records / 4 pages (RESOLUTION_TRUNCATED, never a partial answer). Not for text discovery: call hudu_search.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_get_website",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100; client scan capped at 500 records / 4 pages",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_websites_search",
    "field": "name",
    "newValue": "hudu_search_websites",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_search_websites",
    "field": "title",
    "newValue": "Search Websites",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_search_websites",
    "field": "description",
    "newValue": "Search monitored websites by a free-text query. Preferred over websites.list for any text search. Returns rows: a compact WebsiteSummary (code, message, keyword, \u2026 dropped); expand: true returns the full Website. Bounded: limit defaults to 25 and is hard-capped at 100; a larger value throws CONFIG_ERROR and is never silently clamped. Do not use it when you already have an id, slug or domain: call hudu_get_website instead.",
    "reason": "Description names what it returns, its bound and when NOT to use it (the registry purpose states none of the three)."
  },
  {
    "tool": "hudu_search_websites",
    "field": "annotations.bounded",
    "newValue": "default 25 results, hard maximum 100",
    "reason": "Rule 3: every read tool states a real bound, replacing the mechanical bound (or the \"default ?\" placeholder) with default 25 / hard maximum 100."
  },
  {
    "tool": "hudu_websites_update",
    "field": "name",
    "newValue": "hudu_update_website",
    "reason": "Naming: <prefix>_<verb>_<scope>, verb-first and search-first per the MCP naming standard (a name the example and any consumer can rely on)."
  },
  {
    "tool": "hudu_update_website",
    "field": "title",
    "newValue": "Update Website",
    "reason": "Human-readable display name for tool pickers (the projection has no title field)."
  },
  {
    "tool": "hudu_update_website",
    "field": "description",
    "newValue": "Update a Website. Returns the written record, or the DryRunResult plan when dry_run is true. Impact: one record. Pass dry_run: true to validate without issuing the write (DryRunResult with simulated: true, the impact and the diff). Do not use it to create a record: call hudu_create_website.",
    "reason": "Description states the return shape, the impact bound, the dry-run affordance and the sensitive/approval flags (the registry purpose states none of them)."
  },
  {
    "tool": "hudu_update_website",
    "field": "inputSchema.opts",
    "newValue": {
      "name": "opts",
      "type": "object",
      "required": false,
      "fields": [
        {
          "name": "expectedUpdatedAt",
          "type": "string",
          "required": false
        }
      ]
    },
    "reason": "Dry-run appears once per tool: the injected top-level dry_run is the tool affordance, so the registry opts.dryRun entry is removed as its duplicate."
  },
  {
    "tool": "hudu_operations_searchKnowledge",
    "field": "name",
    "newValue": "hudu_search",
    "reason": "Naming: one self-describing search tool. It is not a knowledge-base reader in two of its three modes (help, resources), so `hudu_search` is the shortest name true for all three; the backing operation stays operations.searchKnowledge (design: .run/design/search/single-search-tool.md 1.1)."
  },
  {
    "tool": "hudu_search",
    "field": "description",
    "newValue": "Search Hudu. One tool, three modes: mode=\"search\" (the default) runs the query; mode=\"help\" returns how to use this tool; mode=\"resources\" returns what is searchable and what each vendor text filter really covers. Only \"search\" needs query.\nmode=\"search\": ranked, fuzzy search over article TITLE and BODY content, asset names, custom-field VALUES and identifiers, plus company/user/group/website names. Each hit carries resource, id, a snippet, a 0-100 score band and the fetch call for the full record. As a TOOL it defaults to tier \"index\" (build or await the body index), so the answer is complete rather than body-blind.\nHonesty: limit default 8 / max 25, snippetChars default 200 / max 400 (0 = none); a bound that bites sets meta.complete false and names itself in meta.reasons. hits=[] is a complete \"nothing matched\" answer ONLY when meta.complete is true and meta.degraded is null - read degraded first: it says when bodies were not searched (\"body-not-indexed\") or the index was cold (\"vendor-only\").\nNot for identity lookup (hudu_find_* / hudu_get_*) or for paging (hudu_list_*). A field the mode cannot honour is a CONFIG_ERROR naming the fields it honours; a missing query is a CONFIG_ERROR naming mode:\"help\", never a silent help fallback.",
    "reason": "Description names the three modes and the default in its first line, the query requirement and the bounds, the tool-level tier:\"index\" default, the honesty contract for degraded/complete, and when NOT to use it."
  },
  {
    "tool": "hudu_search_articles",
    "field": "exclude",
    "newValue": true,
    "reason": "Redundant outcome: hudu_search({mode:\"search\", resources:[...]}) performs this search (or these eight), with body-aware ranking, snippets, company_id scoping and per-resource error isolation - see the migration table in .run/design/search/single-search-tool.md 4.1. Retiring the tool never removes the capability: the backing operation stays callable in the SDK and reachable through hudu_invoke."
  },
  {
    "tool": "hudu_search_assets",
    "field": "exclude",
    "newValue": true,
    "reason": "Redundant outcome: hudu_search({mode:\"search\", resources:[...]}) performs this search (or these eight), with body-aware ranking, snippets, company_id scoping and per-resource error isolation - see the migration table in .run/design/search/single-search-tool.md 4.1. Retiring the tool never removes the capability: the backing operation stays callable in the SDK and reachable through hudu_invoke."
  },
  {
    "tool": "hudu_search_companies",
    "field": "exclude",
    "newValue": true,
    "reason": "Redundant outcome: hudu_search({mode:\"search\", resources:[...]}) performs this search (or these eight), with body-aware ranking, snippets, company_id scoping and per-resource error isolation - see the migration table in .run/design/search/single-search-tool.md 4.1. Retiring the tool never removes the capability: the backing operation stays callable in the SDK and reachable through hudu_invoke."
  },
  {
    "tool": "hudu_search_users",
    "field": "exclude",
    "newValue": true,
    "reason": "Redundant outcome: hudu_search({mode:\"search\", resources:[...]}) performs this search (or these eight), with body-aware ranking, snippets, company_id scoping and per-resource error isolation - see the migration table in .run/design/search/single-search-tool.md 4.1. Retiring the tool never removes the capability: the backing operation stays callable in the SDK and reachable through hudu_invoke."
  },
  {
    "tool": "hudu_search_groups",
    "field": "exclude",
    "newValue": true,
    "reason": "Redundant outcome: hudu_search({mode:\"search\", resources:[...]}) performs this search (or these eight), with body-aware ranking, snippets, company_id scoping and per-resource error isolation - see the migration table in .run/design/search/single-search-tool.md 4.1. Retiring the tool never removes the capability: the backing operation stays callable in the SDK and reachable through hudu_invoke."
  },
  {
    "tool": "hudu_search_websites",
    "field": "exclude",
    "newValue": true,
    "reason": "Redundant outcome: hudu_search({mode:\"search\", resources:[...]}) performs this search (or these eight), with body-aware ranking, snippets, company_id scoping and per-resource error isolation - see the migration table in .run/design/search/single-search-tool.md 4.1. Retiring the tool never removes the capability: the backing operation stays callable in the SDK and reachable through hudu_invoke."
  },
  {
    "tool": "hudu_search_asset_passwords",
    "field": "exclude",
    "newValue": true,
    "reason": "Redundant outcome: hudu_search({mode:\"search\", resources:[...]}) performs this search (or these eight), with body-aware ranking, snippets, company_id scoping and per-resource error isolation - see the migration table in .run/design/search/single-search-tool.md 4.1. Retiring the tool never removes the capability: the backing operation stays callable in the SDK and reachable through hudu_invoke."
  },
  {
    "tool": "hudu_search_password_folders",
    "field": "exclude",
    "newValue": true,
    "reason": "Redundant outcome: hudu_search({mode:\"search\", resources:[...]}) performs this search (or these eight), with body-aware ranking, snippets, company_id scoping and per-resource error isolation - see the migration table in .run/design/search/single-search-tool.md 4.1. Retiring the tool never removes the capability: the backing operation stays callable in the SDK and reachable through hudu_invoke."
  },
  {
    "tool": "hudu_search_across_resources",
    "field": "exclude",
    "newValue": true,
    "reason": "Redundant outcome: hudu_search({mode:\"search\", resources:[...]}) performs this search (or these eight), with body-aware ranking, snippets, company_id scoping and per-resource error isolation - see the migration table in .run/design/search/single-search-tool.md 4.1. Retiring the tool never removes the capability: the backing operation stays callable in the SDK and reachable through hudu_invoke."
  },
  {
    "tool": "hudu_find_activity_logs_by_resource",
    "field": "name",
    "newValue": "hudu_list_activity_logs_for_resource",
    "reason": "Naming: this is NOT an identity lookup - it lists the records attached to one record, while `find_*` promises exactly one record (identity lookups: hudu_find_articles_by_slug, hudu_find_assets_by_serial, ...). `list_*_for_*` says what it does. The operation name is unchanged in the SDK (.run/design/search/single-search-tool.md 4.1)."
  },
  {
    "tool": "hudu_find_expirations_by_resource",
    "field": "name",
    "newValue": "hudu_list_expirations_for_resource",
    "reason": "Naming: this is NOT an identity lookup - it lists the records attached to one record, while `find_*` promises exactly one record (identity lookups: hudu_find_articles_by_slug, hudu_find_assets_by_serial, ...). `list_*_for_*` says what it does. The operation name is unchanged in the SDK (.run/design/search/single-search-tool.md 4.1)."
  },
  {
    "tool": "hudu_find_flags_by_flagable",
    "field": "name",
    "newValue": "hudu_list_flags_for_record",
    "reason": "Naming: this is NOT an identity lookup - it lists the records attached to one record, while `find_*` promises exactly one record (identity lookups: hudu_find_articles_by_slug, hudu_find_assets_by_serial, ...). `list_*_for_*` says what it does. The operation name is unchanged in the SDK (.run/design/search/single-search-tool.md 4.1)."
  },
  {
    "tool": "hudu_find_labels_by_labelable",
    "field": "name",
    "newValue": "hudu_list_labels_for_record",
    "reason": "Naming: this is NOT an identity lookup - it lists the records attached to one record, while `find_*` promises exactly one record (identity lookups: hudu_find_articles_by_slug, hudu_find_assets_by_serial, ...). `list_*_for_*` says what it does. The operation name is unchanged in the SDK (.run/design/search/single-search-tool.md 4.1)."
  },
  {
    "tool": "hudu_find_magic_dash_by_company",
    "field": "name",
    "newValue": "hudu_list_magic_dash_for_company",
    "reason": "Naming: this is NOT an identity lookup - it lists the records attached to one record, while `find_*` promises exactly one record (identity lookups: hudu_find_articles_by_slug, hudu_find_assets_by_serial, ...). `list_*_for_*` says what it does. The operation name is unchanged in the SDK (.run/design/search/single-search-tool.md 4.1)."
  },
  {
    "tool": "hudu_find_relations_by_endpoints",
    "field": "name",
    "newValue": "hudu_list_relations_for_endpoints",
    "reason": "Naming: this is NOT an identity lookup - it lists the records attached to one record, while `find_*` promises exactly one record (identity lookups: hudu_find_articles_by_slug, hudu_find_assets_by_serial, ...). `list_*_for_*` says what it does. The operation name is unchanged in the SDK (.run/design/search/single-search-tool.md 4.1)."
  },
  {
    "tool": "hudu_search",
    "field": "inputSchema",
    "newValue": {
      "mode": {
        "name": "mode",
        "type": "string",
        "required": false,
        "description": "Default \"search\" runs the query; \"help\" explains this tool; \"resources\" lists what is searchable. A field the mode cannot honour is a CONFIG_ERROR naming the fields it honours.",
        "enum": [
          "search",
          "help",
          "resources"
        ]
      },
      "query": {
        "name": "query",
        "type": "string",
        "required": false,
        "description": "search mode only, and REQUIRED there (an empty call is CONFIG_ERROR, never a help fallback)."
      },
      "resources": {
        "name": "resources",
        "type": "array",
        "required": false,
        "description": "search mode only; default [\"articles\",\"assets\"].",
        "items": {
          "type": "string",
          "enum": [
            "articles",
            "assets",
            "companies",
            "users",
            "groups",
            "websites",
            "asset_passwords",
            "password_folders"
          ]
        }
      },
      "topic": {
        "name": "topic",
        "type": "string",
        "required": false,
        "description": "help mode only; default \"core\".",
        "enum": [
          "core",
          "all",
          "query",
          "limits",
          "scoring",
          "degradation",
          "results",
          "followup"
        ]
      },
      "limit": {
        "name": "limit",
        "type": "number",
        "required": false,
        "description": "search mode only; 1-25, default 8."
      },
      "snippetChars": {
        "name": "snippetChars",
        "type": "number",
        "required": false,
        "description": "search mode only; 0-400, default 200."
      },
      "company_id": {
        "name": "company_id",
        "type": "number",
        "required": false,
        "description": "search mode only; one-company scope."
      },
      "updated_since": {
        "name": "updated_since",
        "type": "string",
        "required": false,
        "description": "search mode only; ISO 8601 lower bound."
      },
      "min_score": {
        "name": "min_score",
        "type": "number",
        "required": false,
        "description": "search mode only; 0-100 band floor."
      },
      "exact_only": {
        "name": "exact_only",
        "type": "boolean",
        "required": false,
        "description": "search mode only; true = no fuzzy matching."
      },
      "tier": {
        "name": "tier",
        "type": "string",
        "required": false,
        "description": "search mode only; tool default \"index\" (complete answer); \"auto\" = the SDK default.",
        "enum": [
          "auto",
          "vendor",
          "index"
        ]
      },
      "refresh": {
        "name": "refresh",
        "type": "boolean",
        "required": false,
        "description": "search mode only; true forces an index rebuild."
      }
    },
    "reason": "The tool's contract, flat and self-describing: one optional mode enum (search|help|resources, default search) plus the fields each mode honours, each field stating its mode, its bound and its refusal. Queried by the mode-honours-fields gate."
  },
  {
    "tool": "hudu_search",
    "field": "outputSchema",
    "newValue": {
      "type": "KnowledgeSearchResult (mode-tagged union)",
      "drops": [],
      "dropsUnresolved": false,
      "fields": [
        {
          "name": "mode",
          "type": "string",
          "enum": [
            "search",
            "help",
            "resources"
          ],
          "required": true
        },
        {
          "name": "hits",
          "type": "array",
          "items": {
            "type": "object"
          },
          "required": false
        },
        {
          "name": "meta",
          "type": "object",
          "typeName": "KnowledgeSearchMeta",
          "required": false
        },
        {
          "name": "help",
          "type": "object",
          "typeName": "SearchHelp",
          "required": false
        },
        {
          "name": "resources",
          "type": "object",
          "typeName": "SearchableResources",
          "required": false
        }
      ],
      "fullType": "SearchKnowledgeResult | SearchHelpResult | SearchableResourcesResult (exactly one variant per mode)"
    },
    "reason": "Each mode returns a distinct top-level shape keyed by mode, so a response cannot be misread as another mode's."
  },
  {
    "tool": "hudu_search",
    "field": "annotations.bounded",
    "newValue": "mode=\"search\": limit default 8 / max 25, snippet default 200 / max 400, client scan capped; mode=\"help\"/\"resources\" are static reads with no vendor request",
    "reason": "The bound belongs in the tool's own annotations, not only in its prose."
  },
  {
    "tool": "hudu_get_article_context",
    "field": "outputSchema",
    "newValue": {
      "type": "ArticleContext",
      "drops": [],
      "dropsUnresolved": false,
      "fields": [
        {
          "name": "context",
          "type": "object",
          "typeName": "ArticleContext",
          "required": true
        }
      ],
      "fullType": "ArticleContext",
      "expand": "expand: true returns the full ArticleContext"
    },
    "reason": "Output-schema trim (CORE token budget): the generated field list for this compact shape is replaced by a typed reference. The always-on CORE payload must stay under its 32,000-byte budget, and this record's field-by-field expansion alone was 2.4 KB of the tools/list a client re-reads every turn. Nothing is hidden: the description still states the shape and `expand`, and the full schema stays in the registry (`hudu_describe({operation:\"articles.getContext\"})`) and in capabilities.json."
  },
  {
    "tool": "hudu_find_asset_passwords_by_slug",
    "field": "outputSchema",
    "newValue": {
      "type": "AssetPasswordSummary",
      "drops": [],
      "dropsUnresolved": false,
      "fields": [
        {
          "name": "record",
          "type": "object",
          "typeName": "AssetPasswordSummary",
          "required": true
        }
      ],
      "fullType": "AssetPasswordSummary",
      "expand": "expand: true returns the full AssetPassword"
    },
    "reason": "Output-schema trim (CORE token budget): the generated field list for this compact shape is replaced by a typed reference. The always-on CORE payload must stay under its 32,000-byte budget, and this record's field-by-field expansion alone was 2.4 KB of the tools/list a client re-reads every turn. Nothing is hidden: the description still states the shape and `expand`, and the full schema stays in the registry (`hudu_describe({operation:\"articles.getContext\"})`) and in capabilities.json."
  }
]
