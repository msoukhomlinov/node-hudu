# QA-CONTRACT — independent contract/coverage review of the agent-execution-layer retrofit

Reviewer: contract lens (read-only; nothing under the repo was written except this file).
Branch `feat/agent-execution-layer`. **Pinned review commit: `acc2c59`** (HEAD when this review
started; the tree moved twice under me — `fdeec90` → `ed37986` → `acc2c59` — and further
uncommitted work by other agents was live in the working tree: `src/operations/`, `src/http.ts`,
`src/resources/{exports,public_photos,s3_exports}.ts`, `test/**`, `package.json`, `tsup.config.ts`).
Every claim below is evidence-backed at `acc2c59` unless it says "working tree".
Clean-room evidence tree: `git archive acc2c59 | tar -x -C /tmp/qac/head` (no repo writes).

## Findings (ranked)

**HIGH — `--ship` is RED at HEAD: two plan rows are `planned`, and their declared tests do not exist — `capabilities.plan.json:12818` (`operations.searchAcrossResources`) and `capabilities.plan.json:12884` (`operations.resolveAny`), status `"planned"` at lines 12876/12939 — why it matters: the whole point of the ship gate is "no row may remain `planned` or `implemented`"; commit `fab190a` claims "all 223 plan rows reach tested - ship gate passes", which is no longer true once `ed37986` adds the two operations rows. Any release made from this commit ships a registry that omits two declared operations and a plan that names a test file with no tests. — evidence: in `/tmp/qac/head`, `node scripts/check-capabilities.mjs --ship` → `FAIL — 2 failure(s) … ship-status=2`; `--group operations` → `FAIL … batch-status=2`; `--group A|B|C|D` all PASS. Cross-checked against a full-suite JSON report (1281 executed test names): all 8 test rows of those two rows (`test/operations.test.ts`) are missing; every other one of the 223 rows' 815 declared titles resolves. — fix: finish `src/operations/**` + `test/operations.test.ts`, flip both rows to `implemented` → `tested`, re-run `capabilities:check --ship` (and re-emit the registry) before the Ship gate.**

**HIGH — the additive-only guard has a hole exactly where the retrofit's types live: `test/public-surface.test.ts:29-36` loops over `surface.rootExports.starTypeBarrels`, which the capture script never fills — `scripts/public-surface.mjs:36` regex `^export\s+\*\s+from\s+'\./types/index\.js'` cannot match the baseline's `export type * from …` syntax (`src/index.ts:33`, `src/index.ts:60`), so the fixture records `"starTypeBarrels": []` and the `it('still re-exports the type barrels')` body never runs an assertion. — why it matters: dropping `export type * from './types/index.js'` and/or `…'./resources/index.js'` (the only path to the 35 resource record types and the whole types barrel) breaks every type consumer while the "additive only" test stays green. The retrofit's proof of its own safety claim is therefore partial. — evidence: scratch copy `/tmp/qac/scratch5` (source mutated there, NOT in the repo) removed both star barrels and `export type { Page, ListParams }`; result `1 failed | 5 passed` — the star-barrel test passed; `/tmp/qac/scratch4` (removed root value `CompaniesResource`, renamed `assets.archive`, changed `NOT_FOUND` → `MISSING`) → `3 failed`, so those three checks are real. — fix: capture `export type *` star barrels too (regex `^export\s+type\s+\*\s+from\s+'([^']+)'`) plus the two literal barrel specifiers, and make the type check resolve names from the emitted `.d.ts` instead of a substring search of two files.**

**MEDIUM — `ARCHITECTURE.md` is byte-identical to `main`: the retrofit never points it at the capability matrix, and the review checklist item it owns (policy §11.9 "vendor quirks documented in ARCHITECTURE Special Endpoints + the envelope/capability table") is therefore un-updated — `ARCHITECTURE.md:751-790` (envelope table) is the only capability doc there; `grep -n capabilities ARCHITECTURE.md` → 0 hits — why it matters: policy §4 makes `ARCHITECTURE.md` the place that "points at the matrix for everything else"; a reader of the published artifact (it is in `package.json:72` `files`) learns nothing about plan/registry/gates. In the same table, `ARCHITECTURE.md:777` says `public_photos` has no `singleKey` while `src/resources/public_photos.ts:12` sets `singleKey: 'public_photo'` (pre-existing at `main`, harmless only because `unwrapByKey` passes through a missing key) — the table was claimed re-verified in this review pass and was not. — fix: add a short "capability matrix / gates" section pointing at `capabilities.plan.json`, `src/capabilities.ts`, `capabilities.json`, `capabilities:check`; correct the `public_photos` cell or the constructor (one of the two is wrong).**

**MEDIUM — the committed MCP tool projection is stale and nothing gates it: `MCP_TOOL_MANIFEST.md:4-14` carries `planHash 6808b158…`, `generatedAt 14:05:30`, "registry records: 158 / tools projected: 141", while `capabilities.json` at the same commit has `planHash cd563d7f…`, `generatedAt 14:24:23`, `operationCount 223` — why it matters: the manifest is the MCP deliverable; the committed one omits all 65 helper operations (the agent-facing half of the tier), and references a registry state that no longer exists. `--ship` does not read it and no test references `MCP_TOOL_MANIFEST.md` (`grep -rn MCP_TOOL test/*.ts` → 0), so stale-by-construction is unpoliced. `MCP_TOOL_OVERRIDES.json` does not exist and the manifest says "overrides applied: 0", so if Toolsmith curation happened it is unrecorded. — evidence: working tree already re-projected it (`planHash 2f6e6b2f…`, 14:44:32) — i.e. it is in flight, but the committed artifact is wrong and `.run/RUN-STATE.json` still has `mcp_project: "pending"`. — fix: re-run `npm run mcp:project` as the last step before Ship, commit the result, and add a check (or a test) that the manifest's `planHash` equals `capabilities.json`'s.**

**MEDIUM — the release version was never bumped and the CHANGELOG has no entry: `package.json:3` is still `"version": "0.2.1"` and `CHANGELOG.md`'s newest section is `## [0.2.1]` — why it matters: policy §13 ("Helpers, the capability registry, the `./capabilities` and `./operations` subpaths and new optional parameters are additive → minor bump") and this run's own `SCOPING.md` B ("0.2.1 → 0.3.0, minor") both require a minor bump; additive-but-unbumped breaks the semver contract the retrofit claims to keep. — fix: bump to `0.3.0` and add the CHANGELOG section at the release step (list: helper tier, capability registry, `./capabilities`, new optional params).**

**MEDIUM — the emitted machine-readable capability files are excluded from the published package: `package.json:72` `files: ["dist","examples","README.md","LICENSE","CHANGELOG.md","ARCHITECTURE.md","docs"]` does not include the root `capabilities.json` / `capabilities.schema.json` — why it matters: policy §4.2 puts them at the repo root deliberately "for non-TypeScript consumers"; `files` is a whitelist, so the npm tarball will not contain them and that consumer class cannot read them (only the TS `./capabilities` subpath ships, via `dist`). — evidence: `files` list above; both files exist at the root and are committed. — fix: add `"capabilities.json"` and `"capabilities.schema.json"` to `files` (and, if intended, `capabilities.plan.json`).**

**LOW — 33 of 35 resources' convenience listers are outside the plan and the registry: `node scripts/check-capabilities.mjs --ship` reports `warnings=66` (all `[unplanned-surface]`, e.g. `src/resources/vlans.ts → vlans.listAll`, `…listPages`) — why it matters: the policy calls this a warning, not a failure (checker comment, §4.2 "Discovery rule"), so it is not a violation — but it means the registry and the MCP projection cannot advertise `listAll`/`listPages` at all, and the anti-pattern rule in §12 ("registry maintained by hand in two places / drift") is only half-covered: the source is the drift side. — fix: accept and record the decision (one line in `ARCHITECTURE.md`/`SCOPING.md`), or add the two convenience rows per resource so the surface is fully registered.**

**LOW — the new `./operations` subpath is not wired: `package.json` `exports` has `.`, `./resources`, `./types`, `./errors`, `./capabilities`, `./package.json` (no `./operations`), and `tsup.config.ts:5` lists only `src/capabilities.ts` as the added entry — why it matters: `SCOPING.md` E says "existing subpaths + `./capabilities` (+ `./operations` when present)"; the working tree now has `src/operations/`, so the subpath is due. — fix: add the entry to `tsup.config.ts` and the `./operations` export map when the operations module lands.**

**NIT — the gate's `test-title` rule greps the file text, so a title inside a commented-out or `describe.skip` block would satisfy it (it would not be an executed test). No live defect: I cross-checked all 823 declared test rows against the 1281 *executed* test names from a JSON reporter run — every one of the 223 committed rows' 815 titles resolves verbatim; only the two in-flight `operations` rows' 8 titles do not (see the HIGH above). — fix: make the rule read executed test names (or at least ignore commented regions).**

## VERIFIED OK

- **Coverage spec→plan is complete and exact (item 1).** `api-docs.json` = 82 paths / 158 ops / 35 definitions (matches `SCOPING.md` C). Plan rows with an `endpoint`: 158. Normalised (method + `{param}`-collapsed path) set difference is empty in both directions: `spec ops missing from plan: []`, `plan eps not in spec: []`. Rows total 223 at `acc2c59` (158 primitives + 65 helpers).
- **Every plan primitive/helper names a method that really exists.** Independent strict scan (35 modules, `^\s{2}(public |async |static )*name\s*[(<]`) → `strict missing primitives: []`, `strict missing helpers: []`.
- **`s3_exports` has no helper rows by design, and it is documented.** `SCOPING.md:79` (decision 14: write-only, "no helper rows at all — a documented absence, not an omission") and the module header `src/resources/s3_exports.ts:6`; `capabilities.plan.json` gives it no helper record. Not accidental.
- **Envelope contract honoured — blind behavioural probe, 7/7 pass** (`/tmp/qac/head/test/__probe-contract.test.ts`, written by me, run against the pinned HEAD tree): the nine non-paginated list resources (`ip_addresses, lists, networks, procedure_tasks, rack_storage_items, rack_storages, vlan_zones, vlans, exports`) issue URLs with **no** `page=`/`page_size=` even when `{page:3,page_size:50}` is passed; PUT unwraps by `singleKey` for `companies` (`createType raw`) and `procedures`; `exports.create` and `s3_exports.create` resolve `undefined` on an empty 200; `assets.get(7,9)` → `/companies/7/assets/9` and `assets.list(7)` → `/companies/7/assets` (company scope kept, paging kept); `cards.lookup` unwraps `integrator_cards`; `companies.jump` returns the redirect target; `archive`/`unarchive`/`delete` resolve `undefined`. Code backs it: `src/resources/base.ts:283` ("Always unwrap by singleKey on PUT"), `base.ts:99-102` (strip `page`/`page_size` when `!paginated`).
- **Envelope table ↔ constructor config for all 35 resources.** Brace-aware parse of every `super(http, {…})` vs `ARCHITECTURE.md:755-790`: `singleKey`, `listKey`, `createType`, `paginated` all match, with the single exception noted in the MEDIUM above (`public_photos` `singleKey`), which is pre-existing at `main` and behaviourally inert.
- **Additive-only guard is REAL for values, methods and error codes (item 3).** Scratch mutations in `/tmp/qac/scratch4` (repo untouched): removing `CompaniesResource` from `src/index.ts`, renaming `assets.archive`, changing `NOT_FOUND`→`MISSING` → `3 failed | 3 passed`, each failure naming the removed item. It is VACUOUS for the star type barrels (the HIGH above). Mutating the *fixture* instead of the source proves nothing (both scratch runs of that shape passed) — the fixture is a subset assertion.
- **The fixture is a faithful baseline capture, not a post-retrofit snapshot.** Re-derived independently from `git show main:`: 69 value exports, 7 explicit type exports, 35 resource classes / 259 methods, 11 error codes — zero differences in either direction vs `test/__fixtures__/public-surface.json`.
- **Pre-existing behaviour preserved (item 5).** `git diff main acc2c59 --numstat -- test/` → **no test file has a single deleted line**; `test/base|client|config|errors|http|logger|pagination|utils.test.ts` and `test/resources/{assets,companies,crud-resources,procedures,special,uploads}.test.ts` are unchanged or addition-only; `assets/companies/procedures/uploads` gained test cases (`A`/`M` status). No `.skip`, `.only` or `.todo` anywhere in `test/` (`grep -rn`). Running the 14 pre-existing files: **549 passed / 549**.
- **Test-row titles verified against executed tests, not text (item 4).** Sampled/checked all rows: 823 declared `{file,title}` rows; 815/815 of the committed 223 rows matched a name in the executed-test set of a full-suite run (1281 cases), verbatim. The only misses are the 8 titles of the two `operations` rows (their file `test/operations.test.ts` did not exist at `acc2c59`).
- **The checker is itself gated and the negative fixture is real.** At `acc2c59`: `node scripts/check-capabilities.mjs --plan test/fixtures/capabilities.plan.drifted.json` → exit **1**, failing 4 distinct rules (`missing-key`, `mutation-dryRun`, `test-title`, `impact-bound`). (Note: `… | tail` masks the exit code — read `$?` directly, as done here.)
- **Emission consistency at `acc2c59`.** `sha256(capabilities.plan.json)` = `cd563d7f…` equals `capabilities.json` `planHash`; `operationCount 223` = 225 rows − 2 `planned`, which is exactly what §4.2 prescribes ("planned rows … get no record").
- **Group gates.** `--group A|B|C|D` → PASS (77/57/42/47 rows scoped, 0 failures) at `acc2c59`.

## UNVERIFIED / out of scope

- **UNVERIFIED:** multi-hop redirect behaviour of `jump`/`followRedirect` (my stub returned a single 302 → final URL); retry/`Location` edge cases are untested by me.
- **UNVERIFIED:** `npm pack` contents and the real coverage thresholds (97/94/83/97) — both forbidden by the brief; the full suite run *without* coverage passed (48 files, 1281 tests).
- **UNVERIFIED:** the two `operations` rows' tests and status — `src/operations/**` and `test/operations.test.ts` are another agent's in-flight work (per brief, out of scope); they are the cause of the HIGH ship-gate finding.
- **Out of scope:** `src/operations/**`; the uncommitted working-tree edits to `src/http.ts`, `src/resources/{exports,public_photos,s3_exports}.ts`, `test/**`, `package.json`, `tsup.config.ts` made by other agents during this review.
