# QA — registry record metadata completeness (src/capabilities.ts)

Branch `feat/agent-execution-layer` @ `fdeec90` (registry is clean in git; generated by scripts/generate-capabilities.mjs).
Registry object spans lines 43–267: 223 records, one JSON object literal per line. Verified by parsing lines 45–266 as JSON (223 parsed, 0 parse failures).

## Findings (ranked)

- **CRITICAL — `expectedUpdatedAt` is advertised and demonstrated on create/delete/archive, where the SDK throws CONFIG_ERROR — src/capabilities.ts:45, :51, :52, :53, :60, :62 (and 58 more) — an agent that copies the record's ONLY example gets `HuduConfigError` and no request is issued (`src/resources/base.ts:389-396`, called from `createOne` :213, `deleteOne` :296, `setArchived` :328), yet 0 of those 64 records list CONFIG_ERROR in `errors` — fix: drop `expectedUpdatedAt` from the inputSchema and example of every non-`update` primitive, or (if intentionally documented) add CONFIG_ERROR to `errors` and make the example a valid call.**
  - Measured: 64 records have an op named create/delete/archive/unarchive/deleteAll *and* `expectedUpdatedAt` in `inputSchema`; all 64 pass `expectedUpdatedAt` in `examples[0]` (94 records declare the field in total, 94 examples pass it). 65 records list CONFIG_ERROR; **0** of the 64 guard records do.
  - Examples: `activity_logs.deleteAll` :45 `"examples":["await hudu.activity_logs.deleteAll({ datetime: 'example', delete_unassigned_logs: true }, { dryRun: true, expectedUpdatedAt: 'example' })"]`; `articles.archive` :51; `articles.create` :52; `articles.delete` :53; `articles.unarchive` :60; `asset_layouts.create` :62; `expirations.delete` :106; `flag_types.delete` :116; `magic_dash.create` :165; `networks.create` :177.
- **HIGH — 79 `related` edges point at capability names that do not exist in the registry — src/capabilities.ts:45, :46, :47, :48, :90, :99, :106, :107 — `related` is the agent's "what do I call next" graph; an unresolvable name sends the agent to a capability `getCapability(name)` returns `undefined` for — fix: emit `related` only for names present in CAPABILITY_REGISTRY, and either add plan rows for the real methods or repoint to the nearest registered op.**
  - Measured: 79 records hold >=1 dangling edge; 69 distinct dangling targets. 15 targets (24 refs) are real public SDK methods missing from the registry (e.g. `activity_logs.listAll` @ `src/resources/activity_logs.ts:78`, `users.listAll` @ `src/resources/users.ts:112`, `magic_dash.listAll` @ `src/resources/magic_dash.ts:58`) — the registry under-describes the SDK's public surface (33 `listAll` methods exist in `src/resources/*.ts`). 54 targets (55 refs) name methods that do **not** exist anywhere in the SDK: `expirations.archive` (:106), `expirations.get` (:107), `cards.get` (:90), `flag_types.archive`, `vlans.archive`, `api_info.list`, `asset_layouts.search`, and ~48 more `.archive`/`.search` names.
- **HIGH — `inputSchema` fields omit the `name` key that the file's own `CapabilityField` interface declares — src/capabilities.ts:11-15 (declaration) vs :46, :48, :50, :51, :53, :54, :55, :56, :58 — 138 records carry 156 input fields shaped `{"type":"number","required":true}` with no `name`; a consumer doing `field.name` (or an MCP tool schema built from it) silently gets `undefined` for 7% of all fields (156/2147) — fix: emit `name` on every field as the other 85 records do, and make the generator enforce one shape.**
  - Measured: 2147 field nodes scanned (`inputSchema` values + `outputSchema.fields`); 156 missing `name`, all in `inputSchema`, across 138 distinct records; `outputSchema.fields` never omits `name`.
- **HIGH — the `errors` vocabulary omits codes the SDK actually raises — src/capabilities.ts (all 223 `errors` arrays) — a caller cannot tell which failures are retryable/terminal; the strongest case is STALE_OBJECT: 94 records declare the `expectedUpdatedAt` guard, `src/resources/base.ts:530` throws `StaleObjectError` when the guard mismatches, and 412 is mapped at `src/errors.ts:294` — yet no record lists STALE_OBJECT — fix: derive `errors` from the raise sites, adding at least STALE_OBJECT (guarded ops), FORBIDDEN (403, `src/errors.ts:289`), CONFLICT (409, :291), VALIDATION_FAILED (:224), DUPLICATE_FOUND (:233), METHOD_NOT_ALLOWED (:181), SERVER_ERROR (:203), TIMEOUT and `HTTP_<status>` (:299).**
  - Measured (C6 spot-check of 6 distinct codes + reverse check): registry codes = 11 distinct, all SCREAMING_SNAKE, all with real raise sites — NETWORK_ERROR (`src/errors.ts:155`), UNAUTHORIZED (:166), UNPROCESSABLE_ENTITY (:191), NOT_ACCEPTABLE (:186), BAD_REQUEST (:161), RATE_LIMIT (:197), CONFIG_ERROR (:143), POLICY_DENIED (:274), NOT_FOUND (:176), RESOLUTION_TRUNCATED (:260), RESOLUTION_AMBIGUOUS (:265). Frequency: NETWORK_ERROR 223, NOT_FOUND 160, UNAUTHORIZED 153, UNPROCESSABLE_ENTITY 86, CONFIG_ERROR 65, RESOLUTION_TRUNCATED 54, RESOLUTION_AMBIGUOUS 47, BAD_REQUEST 13, RATE_LIMIT 8, POLICY_DENIED 3, NOT_ACCEPTABLE 1. No registry code is unraised; 8 raised families are missing from every record.
- **MEDIUM — three helpers set `compact` but publish an empty `drops` list while asserting the drops were resolved — src/capabilities.ts:56 (`articles.getContext` → `ArticleContext`), :82 (`assets.getContext` → `AssetContext`), :99 (`companies.getContext` → `CompanyContext`) — each has `"drops":[]` with `"dropsUnresolved":false`; the record therefore claims "the compact shape names what it throws away" and names nothing, so an agent has no warning that `ArticleContext` returns summaries, not full records — fix: fill `drops` from the helper's return type, or set `dropsUnresolved:true` so the gap is visible instead of asserted-clean.**
  - Measured (C7): 58 records have `compact`; 55 have non-empty `drops`; 3 are empty; 0 records have `dropsUnresolved:true`.
- **MEDIUM — pagination says `mode:"page"` but no record ever exposes the page controls the SDK accepts — src/capabilities.ts:47, :57, :64 (23 records) — `src/pagination.ts:14-19` documents `page`/`page_size` as "Parameters understood by every list method", but 0 of 223 records expose `page`, `page_size`, `pageSize` or `per_page` in `inputSchema`, so an agent must re-derive paging from the SDK rather than the registry — fix: add `page`/`page_size` to the inputSchema of the 23 `mode:"page"` records (and state whether `listAll`-style helpers auto-paginate).**
  - Measured (C4): 0 records with `nonPaginated:true` and `mode != "none"`; 0 with `mode:"none"` and `nonPaginated:false`; 200 `none` / 23 `page`; 23 page-mode records expose no page parameter.
- **LOW — pagination metadata has two shapes — src/capabilities.ts:47 (`crossCheck`/`sourcePaginated`/`specSaysNonPaginated` present) vs :46 (`activity_logs.findByResource`, absent) — a consumer cannot tell "no cross-check was run" from "cross-check unavailable" — fix: emit the three cross-check keys on every record (191 have 4 keys, 32 have 7).**
- **LOW — `permissions` is present on every record but never informative — src/capabilities.ts (all 223) — 223/223 are the literal `"unknown"`, so the field cannot gate anything; acceptable per the C2 criterion (key present), but it should be marked nullable/optional or populated — fix: populate from the plan's permission mapping or accept the placeholder explicitly in the contract.**
- **LOW — examples are schema-shaped, not realistic — src/capabilities.ts:48, :57, :64 (and 170 more) — every record has exactly 1 example and 173/223 use the literal placeholder `'example'` for strings and `1` for ids, so a copied example is not runnable as documentation — fix: keep the single example but seed real-looking values from the fixture set.**
- **NIT — the header's "no side effects" claim is true, but the file does make one top-level call — src/capabilities.ts:272 `export const CAPABILITY_NAMES = Object.keys(CAPABILITY_REGISTRY) as CapabilityName[]` — it is pure and allocation-only (no I/O, no mutation), so tree-shaking and zero-import constraints hold — fix: none required; if the literal "no top-level call" rule is contractual, wrap it in a function.**

## VERIFIED OK

- C1 — every record has >=1 example: parsed all 223 records and counted `examples`; `Counter({1: 223})`; 0 records with an empty `examples` array. Command: `python` parse of `src/capabilities.ts` lines 45-266 → `Counter({1: 223})`, `no_examples=0`.
- C2 — `permissions` key present on all 223 (`missing_key=0`), value `unknown` for 223/223 (placeholder, see LOW above).
- C3 — `preferredWhen` null/absent = **0 of 223**; every record that shares a resource with a sibling (34 of 35 resources have >1 op; 34 have get/resolve/find/search siblings) has a non-null `preferredWhen`. Spot-read of 22 mechanically-detected mutual "prefer" pairs (e.g. `articles.archive` :51 vs `articles.delete` :53; `articles.list` :57 vs `articles.search` :59) shows they are conditional and mutually consistent ("preferred when the record may be needed again" / "Irreversible; prefer archive"), not contradictory. Only 2 `preferredWhen` strings are duplicated across records (max 25 copies: "Preferred over create or delete when the record exists and only some fields change.").
- C4 — 0 records claim a page mode for a non-paginated endpoint (`nonPaginated:true` AND `mode!="none"` = 0; `mode:"none"` AND `nonPaginated:false` = 0).
- C5 — `retry` present on 223/223; all 223 share one shape `{retryableStatuses:[429,500,502,503,504], backoff:"exponential", baseDelayMs:200, maxDelayMs:30000, jitter:"up-to-25%-of-delay", idempotencySupport}`; `idempotencySupport` is `none` for 160 and `vendor-native` for 63, and conflicts with the `idempotent` flag on **0** records (63 `idempotent` flags == 63 `vendor-native`).
- C6 (positive half) — all 11 registry codes are SCREAMING_SNAKE (`non_screaming=0`) and every one has a raise site in `src/errors.ts` / `src/resources/base.ts` (see mapping above). No record has an empty `errors` array.
- C6 (drift half) — plan vs registry are in sync: `capabilities.plan.json` has 225 rows, of which 223 are `status:"tested"` and 2 are `status:"planned"` (`operations.searchAcrossResources`, `operations.resolveAny`, both plan-only). Registry keys == the 223 tested names exactly (set difference empty both ways), and 0 mismatches across 10 fields compared per row (purpose, usage, preferredWhen, related, effect, flags, dryRun, errors, compact, resolution). The 225-vs-223 count is expected, **not** generation drift.
- C7 (positive half) — 55 of the 58 `compact` records have non-empty `drops`; `dropsUnresolved` is false on 223/223.
- C8 — `rg -n '^import |require\(|^const .*=' src/capabilities.ts` returns only line 272 (`Object.keys`); 0 `import`, 0 `require`, 0 other top-level statements. The module is import-free and side-effect-free as claimed.
- Structural cross-checks with 0 defects: `effect` null = 0 (`read` 129 / `write` 66 / `destructive` 28); destructive without `requiresApproval` = 0 (29 records flagged); `dryRun:true` on a `read` record = 0; `related` self-reference = 0; dangling `outputSchema` names = n/a (no name-less output fields); `kind` = primitive 158 / helper 65; 3 `getContext` helpers have `resolution:null` (the only 3) — consistent with "no resolver", flagged only as a shape observation.
