# QA — FINAL CONTRACT review (node-hudu "agent execution layer" retrofit)
Branch `feat/agent-execution-layer` @ **ca011f1** (`git rev-parse HEAD` = ca011f16a0e26feb0214bcb5e73e074c6dba12a7), baseline `main` @ 9332efe (v0.2.1). 28 commits, 163 files, +62018/-368.
Reviewer: final contract lens (read-only; only this file written). All falsification done in clean `git archive HEAD` trees under /tmp (`/tmp/qafc2/head` = pristine commit; `/tmp/qafc2/v1..v5` = deliberate breaks, repo untouched). 55-call budget; ~45 grounding calls used.

## Scope covered
Whole branch (not one group): plan/registry/emitter, gates, public-surface guard, test integrity, envelope contract, ARCHITECTURE/README/CHANGELOG/package metadata, MCP projection, error contract.

## FINDINGS (ranked)

- **MEDIUM — The additive-only guard is still blind to the *individual* record types, i.e. to every one of the 29 record-type names present in the baseline fixture — `test/public-surface.test.ts:38-43` (test "still exports every name in the types barrel") — why it matters: the check is `barrel.includes(name)`, a plain substring search of `src/types/index.ts`. Every record type is a prefix of a derived name in the same file (`Company` ⊂ `CompanyCreate`/`CompanySummary`/`CompanyUpdate`, `Article` ⊂ `ArticleCreate`…), so deleting `export type { Company } from './company.js';` leaves the name present as a substring and the whole suite stays GREEN. The retrofit's central safety claim ("additive only, proven by test/public-surface.test.ts") therefore has no per-type force: a shipped type removal passes CI. The earlier wholesale hole is fixed (see VERIFIED OK), this narrower one is not. — evidence (clean /tmp copies, repo untouched): v5 removed `export type { Article } from './article.js';` → `Tests 8 passed (8)`; v4 removed `export type { ActivityLog } …` → `8 passed (8)`; control v3 removed `export type { IpAddressCreate } …` → `1 failed | 7 passed` with `removed type-barrel exports: IpAddressCreate`. Static cross-check over the 113 fixture barrel names: 44 are substrings of another exported name; of the 29 record types in the fixture, **29/29 are sub-classes of that hole (0 protected)**. No live break exists: independently parsed, all 113 baseline names are still present in `src/types/index.ts` export lists at ca011f1. — fix: replace `includes` with a line/TS-AST match (`^export\s+(?:type\s+)?\{\s*Name\s*(?:\s+as\s+\w+)?\s*\}` per file), and resolve the names from the emitted `.d.ts` so the guard survives reformatting.

- **MEDIUM — `MCP_TOOL_OVERRIDES.json` is not in the released commit; the committed manifest ships "overrides applied: 0" — `MCP_TOOL_MANIFEST.md:11` (committed blob), `scripts/project-mcp-tools.mjs:42` (reads it), `git ls-tree HEAD` (absent) — why it matters: curation is defined to live ONLY in that file (`MCP_TOOL_MANIFEST.md:5,17,29`; script comment line 27), and the file is the 1260-delta curation of the MCP tool surface. At ca011f1 it does not exist, so the committed projection is the uncurated one: 203 tools, 59 read tools still backed by a plain primitive, 2 with no helper-tier backing (`hudu_operations_resolveAny`, `hudu_operations_searchAcrossResources`). A release cut from this commit ships the uncurated manifest; `.run/RUN-STATE.json:34` still says `"mcp_project": "in_progress"`. Re-projecting from the commit reproduces the committed files byte-for-byte (except the timestamp line), so this is not staleness — it is a missing artifact. — caveat: the same in-flight regeneration that owns `examples/mcp-server.ts` is producing this file right now (it exists untracked in the working tree). — fix: `git add MCP_TOOL_OVERRIDES.json`, re-run `npm run mcp:project`, commit manifest + example + overrides together, then re-run `capabilities:check` so the new `manifest-planhash` rule gates the result.

- **LOW — `ARCHITECTURE.md`'s authoritative envelope table still records `public_photos` `singleKey` as `–` (pass-through) while §12.23 of the same file and the constructor say `public_photo` — `ARCHITECTURE.md:777` (row `public_photos | – | public_photos | raw | yes | no`) vs `src/resources/public_photos.ts` (`singleKey: 'public_photo'`) and `ARCHITECTURE.md:696`; the vendor spec agrees with the code: `/public_photos/{id}` 200 body carries a `public_photo` key (`api-docs.json:8043`) — why it matters: the table is labelled "authoritative; reviewer must re-verify against spec", so a reader trusts the wrong cell; the retrofit added the capability section to this file but did not re-verify this pre-existing row. **Behaviour is inert** (`unwrapByKey` returns the body unchanged when the key is absent: `src/http.ts:140-143`), so no runtime defect. — fix: set the cell to `public_photo` (or delete the `–` convention for that row).

- **LOW — A plan/registry row uses a `resolution.basis` value the contract does not define, and the gate only warns about it — `capabilities.plan.json:8475` (`procedures.getWithTasks` → `"basis": "composite"`), registry `capabilities.json` mirror; gate emits `! [resolution-basis]` (1 of 67 warnings) — why it matters: the policy contract is `null | { "basis": "server-filter" | "client-scan", … }` (api-node-squad `references/agent-execution-layer.md:316`), `capabilities.schema.json` constrains no `basis` enum at all, and `--ship` PASSes with only a warning — so an agent consumer that switches on `basis` hits an undocumented third value in a shipped artifact. — fix: add `composite` to the schema enum + the reference table, or re-classify the row.

- **NIT — The group gates as specified (A|B|C|D) scope 223 of the 225 rows; the two `operations.*` rows live in group `operations` — `capabilities.plan.json` rows `operations.searchAcrossResources` / `operations.resolveAny` (`"group": "operations"`); `--group A|B|C|D` reports scoped=77/57/42/47 — why it matters: the batch-gate procedure in the brief would leave the newest two rows uncovered at batch level (they are covered by the full run and `--ship`). This is documented design (SCOPING.md decision 13: "`operations` for a cross-resource helper in `src/operations/`") and the checker accepts `--group operations` (`PASS … scoped=2`), so it is a procedure note, not a defect. — fix: add `--group operations` to the documented gate sequence, or reject a group outside `{A,B,C,D,operations}`.

- **NIT — `HuduError.status` became a prototype getter, so an own-property spread of an error no longer carries `status` — `src/errors.ts` (`private readonly _status` + `get status()`/`get httpStatus()`), main had `readonly status?: number` as a data field — why it matters: `.status` reads are unchanged and `httpStatus`/`vendorError` were added (all additive at the type level; pre-existing `test/errors.test.ts` is untouched and passes), but `{...err}.status` / `Object.keys(err)` no longer include `status` — a silent change for any consumer serialising errors with spread. — fix: assign an own `status` data field (keeping the getter alias), or record it in the CHANGELOG migration note.

- **NIT — The `test-title` gate rule still greps source text, so a title inside a skipped/commented block would satisfy it — `scripts/check-capabilities.mjs:422-423` (`titleExists(t.file, t.title)` reads the file) — why it matters: the rule is the only guard that a `tested` row is genuinely tested. No live defect at this commit: I checked all 823 declared `{file,title}` rows against the **executed** test names of a JSON-reporter run in the clean tree (49 files, 1500 tests, 1500 passed) → **0 misses**. — fix: have the rule read executed names (or at least strip commented regions / `describe.skip`).

## VERIFIED OK (command → result)

- **Pin, ship gate and all gates at ca011f1** (run in the pristine `/tmp/qafc2/head` copy of the commit, `echo $?` read without a pipe): `node scripts/check-capabilities.mjs` → exit 0, `PASS — 0 failures; rows=225 scoped=225 registryRecords=225 warnings=67`; `--group A|B|C|D` → exit 0, scoped 77/57/42/47; `--group operations` → exit 0, scoped=2; `--ship` → exit 0, `PASS — 0 failures; rows=225 scoped=225 registryRecords=225 warnings=67`; `--plan test/fixtures/capabilities.plan.drifted.json` → **exit 1**, `FAIL — 3 failure(s) in 3 distinct rule(s): missing-key, mutation-dryRun, test-title`.
- **Previous HIGH "ship gate RED, 2 planned rows" → CLOSED.** `Counter(status) = {'tested': 225}` over `capabilities.plan.json.operations`; `capabilities.json.operationCount = 225`.
- **All 158 spec operations have a row and a real primitive (item 1 of the focus).** Rows with `endpoint`: 158; spec ops: 158; normalised (method + `{param}`-collapsed path) set difference empty in both directions (`spec not in plan: []`, `plan not in spec: []`); rows with `helper`: 67. Strict parse of the 35 `src/resources/*.ts` modules → every one of the 223 `resource.method` names resolves to a real definition; the only 2 not in `src/resources` are `operations.searchAcrossResources`/`operations.resolveAny`, which are defined in `src/operations/operations.ts`. No row is a phantom.
- **Every declared test title is a real executed test.** `/tmp/qafc2/head` `npx vitest run --reporter=json` → 49 files / 1500 tests / 1500 passed; matching all 823 declared `{file,title}` pairs of the 225 rows against the executed names → **0 missing**. (Stronger than the required 15-row sample; covers both helper and primitive rows across all groups.)
- **Additive-only guard: the wholesale hole from the last review is FIXED and proven by breaking a barrel outside the repo.** `/tmp/qafc2/v1` deletes `export type * from './types/index.js';` from `src/index.ts` → `× still re-exports every star barrel` (`1 failed | 7 passed`); `/tmp/qafc2/v2` deletes `export type * from './resources/index.js';` → same failure. The capture script now records both barrels (`scripts/public-surface.mjs`, regex accepts `export type *`), and `test/public-surface.test.ts` "the guard itself is not vacuous" asserts ≥1 star barrel / ≥30 barrel names / 35 classes — the vacuity is closed. Values, methods and error codes remain protected (v3 control fails as expected; earlier scratch runs for those are reproduced by the non-vacuity assertions + the 113-name parse). **Residual per-type hole is the MEDIUM above.**
- **Fixture is a faithful 0.2.1 baseline.** `test/__fixtures__/public-surface.json`: `capturedFrom "main (0.2.1 baseline)"`, 69 root values, 7 explicit type exports, starBarrels `['types/index.js','resources/index.js']`, 113 type-barrel names, 35 resource classes, 11 error codes; all 113 names still present in `src/types/index.ts` at HEAD.
- **No pre-existing test was weakened.** `git diff main...HEAD --numstat -- test/` → **0 deleted lines in any test file**; only 4 pre-existing files changed and each is addition-only (`assets`, `companies`, `procedures`, `uploads`); `grep -rnE '\.(skip|only|todo|fails)|xit\(|xdescribe\(' test/` → no hits; the pre-existing coverage thresholds are unchanged (`97/94/83/97` in `vitest.config.ts`, untouched in the diff).
- **Envelope contract still holds — blind behavioural probe re-run on the ca011f1 tree: 7/7 pass.** Same probe as the earlier review (my own file, `test/__probe-contract.test.ts`, copied into the clean tree): the 9 non-paginated resources (`ip_addresses, lists, networks, procedure_tasks, rack_storage_items, rack_storages, vlan_zones, vlans, exports`) issue **no** `page=`/`page_size=` even when passed; PUT unwraps by `singleKey` for `companies` (raw `createType`) and `procedures`; `exports.create`/`s3_exports.create` resolve `undefined` on an empty 200; `assets.get(7,9)` → `/companies/7/assets/9`, `assets.list(7)` → `/companies/7/assets`; `jump`/`lookup` follow redirects; `archive`/`unarchive` return void.
- **`ARCHITECTURE.md` now points at the capability matrix (previous MEDIUM → CLOSED).** It gained a matrix section naming `capabilities.plan.json` ("authored source of record"), `src/capabilities.ts` (generated runtime registry, `node-hudu/capabilities`), `capabilities.json`/`capabilities.schema.json` ("emitted data + JSON Schema at the package root, for non-TypeScript consumers, pin-checked by `planHash`"), `scripts/*.mjs`, the gate and its committed negative fixture. `README.md` names the registry and the `node-hudu/capabilities` subpath (5 hits).
- **Release hygiene (previous MEDIUMs → CLOSED).** `package.json` version `0.3.0`; `CHANGELOG.md` `## [0.3.0] — 2026-09-13`; `files` now includes `capabilities.json`, `capabilities.schema.json`, `MCP_TOOL_MANIFEST.md`; `exports` includes `./capabilities` and `./operations` (previous LOW → CLOSED); `tsup.config.ts` builds both entries.
- **Emission is self-consistent.** `sha256(capabilities.plan.json)` = `65c7cab6af926eec…` = `capabilities.json.planHash`; `operationCount 225`; the gate's own `emission-planhash`/`manifest-planhash` rules pass.
- **The committed MCP projection is in sync and is now gated (previous MEDIUM "stale, unpoliced" → CLOSED).** A fresh `node scripts/project-mcp-tools.mjs` from the committed tree reproduces the committed `MCP_TOOL_MANIFEST.md` and `examples/mcp-server.ts` byte-for-byte except the timestamp line; `node scripts/project-mcp-tools.mjs --check-example` → exit **0**; the checker now carries a `manifest-planhash` rule (`scripts/check-capabilities.mjs:57,90,530`) that fails on a stale manifest and passed here. (`planHash 65c7cab6…`, 225 registry records, 203 tools.)
- **`public_photos` singleKey: still inert.** `unwrapByKey` returns the body unchanged when the key is missing (`src/http.ts:140-143`), so the table/constructor disagreement above cannot corrupt a response.

## UNVERIFIED
- **UNVERIFIED:** `npm pack` + clean-dir ESM/CJS import of the 5 subpaths and the shipped 0.3.0 payload — I relied on the coordinator's measurement; I did not re-run it (my `files`/`exports` inspection is consistent with it).
- **UNVERIFIED:** the coverage figures (99.65 / 99.89 / 92.49 / 99.27 vs thresholds 97/94/83/97) — I ran the suite without coverage (1500/1500), not `--coverage`.
- **UNVERIFIED (by design of the guard):** signature-level additivity. The fixture proves name/code existence, not parameter lists. I spot-checked the 262 deleted lines in `src/` (mostly `createOne`/`updateOne` delegate bodies replaced by richer ones) and read the `src/errors.ts` deltas (all constructors kept their positional parameters, optional ones appended) — but a widened/narrowed parameter type elsewhere would not be caught by the guard.
- **UNVERIFIED:** semantic correctness of `resolution` (server-filter vs client-scan behaviour) beyond the basis/enum warning; and the 225 rows' `compact`/`redaction`/`staleCheck` fields beyond spot checks (`fullType`, `compact`, `tests`, `errors` sampled).
- **Out of scope (in flight, half-written in the working tree — do not read as defects):** `examples/mcp-server.ts`, `MCP_TOOL_MANIFEST.md`, `scripts/project-mcp-tools.mjs` (all modified) and the untracked `MCP_TOOL_OVERRIDES.json`. Note for the release cut: the working tree is dirty (3 modified + 1 untracked), so a release taken from the working tree is not the artifact set this review validated; cut from ca011f1 + the finisher's commit, or re-run the gates after the MCP work lands.

## What I tried to falsify (and how)
1. **Vacuity of the additive-only proof** — broke star barrels and record types in /tmp copies: barrel breaks now FAIL (fixed), per-type removals still PASS (new MEDIUM).
2. **A row that is not really tested/shipped** — tried to find a `planned`/`implemented` row (none: 225/225 `tested`) and a declared title that does not execute (823/823 execute).
3. **Spec drift** — tried to find a spec op without a row or a row whose primitive does not exist (none in either direction; 223/223 method names resolve).
4. **Weakened pre-existing tests** — looked for deleted test lines, `.skip/.only/.todo`, relaxed thresholds (none).
5. **Envelope regression** — blind behavioural probe on the committed tree (7/7 pass, all five invariants from the focus list).
6. **Stale/unpoliced MCP manifest** — regenerated from the commit and diffed (identical), and confirmed a gate rule now exists.
7. **Semver/additive-metadata drift** — version, CHANGELOG, `files`, `exports`, planHash (all consistent).

## Verdict
CONDITIONAL PASS. The contract is intact and the additive-only claim is proven for the value/behaviour surface, the envelope surface, the test surface and the spec→plan→registry→MCP chain — all gates PASS at ca011f1 and the previous review's two HIGH findings are CLOSED (verified by re-derivation, not by report). Two release blockers-adjacent items: (a) the release commit does not contain `MCP_TOOL_OVERRIDES.json`, so the curated MCP surface is not in the branch (the running MCP finisher must commit it and re-project); (b) the additive-only proof is still toothless for every individual record type in the types barrel. One lens, one clean-ish verdict: I could not falsify the 158-op coverage, the envelope table, the test integrity or the ship gate.
